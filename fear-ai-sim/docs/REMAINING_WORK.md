# Fear AI / BadAI — Remaining Work Ledger

**Date:** 2026-08-26  
**Repository:** `fear-ai-sim/`  
**Baseline:** historical baseline was 540 tests; latest verification is 24 suites/576 tests passed, Vite build passed with import/chunk warnings, and npm audit reported 0 vulnerabilities.

Statuses are deliberately evidence-based:

- `IMPLEMENTED_AND_VERIFIED`
- `PARTIALLY_IMPLEMENTED`
- `PROPOSED`
- `UNKNOWN`
- `BLOCKED_EXTERNAL`

| Area | Status | Evidence | Remaining verification/work |
|---|---|---|---|
| Repository grounding and baseline | IMPLEMENTED_AND_VERIFIED | Current checkout, package scripts, 13 Jest suites/540 tests, Vite build, audit all reproduced | Keep baseline current after changes |
| Browser/Electron/Tauri entry surfaces | PARTIALLY_IMPLEMENTED | `index.html` → `main.js` → `simulation.js`; wrappers/config present | Verify packaged runtime paths and native release builds |
| FearCore transition contract | PARTIALLY_IMPLEMENTED | `fearcore.js`, unit tests, Brain integration for reactive path | Establish authoritative scale/source, migrate all fear paths, add parity vectors |
| Hysteresis ownership | PARTIALLY_IMPLEMENTED | FearCore owns one reactive transition path; legacy `hysteresis.js` exists and is tested | Prove no production duplicate ownership and document migration |
| Habituation ownership | PARTIALLY_IMPLEMENTED | Brain has inline habituation; standalone `habituation.js` exists/tests | Select one runtime owner and test deterministic stimulus/session semantics |
| Brain/personality/morale | PARTIALLY_IMPLEMENTED | Brain traits, fear, anger, dominance, morale and movement are live/tested | Separate stable traits/moods and prove downstream decision consequences |
| Planning and behavior execution | PARTIALLY_IMPLEMENTED | Planner/actions/behavior tree exist; Brain invokes the tree; `ExecutePlanNode` now revalidates preconditions and records outcomes; integration tests pass | Add registered affordances, utility/confidence/explanation traces, and authoritative resource/world consequences |
| Beliefs/evidence/rumors/reputation | PARTIALLY_IMPLEMENTED | `beliefs.js` provides explicit layers, weighted evidence, conflicts, decay, rumors, and serialization; Simulation records predator-kill evidence and the opt-in closed-world path derives survivor evidence/public rumor | Add observer-specific propagation, reputation effects, and broader downstream decisions |
| Character interactions and authoritative mutation | PARTIALLY_IMPLEMENTED | `interactions.js` provides deterministic action registry, hard validation gates, cooldowns, and authoritative consequences; `Simulation.executeInteraction()` is wired and `tests/simulation-social-loop.test.js` passes | Add default actor population, norm, witness, and retaliation integration |
| Routing/trade/economy | PARTIALLY_IMPLEMENTED | `routing.js`, `economy.js`, and `trade.js` provide deterministic route/market/trade behavior; `runTradeTrip()` remains immediate; `startTrip()` snapshots immutable origin/destination/path/towns/cargo and `edgeIndex` without market mutation; `completeTrip()` advances one edge and delivers once on the final edge; concrete `Simulation` retains the real town map; focused and full tests pass | Add rumor/faction feedback and closed-world consequences |
| Convoys/escorts/bandits | PARTIALLY_IMPLEMENTED | `convoy.js` and `closed-world.js` now form deterministic convoys, assign escorts, resolve ambush loss, and update bandit expectations; focused tests pass | Add relocation policy, survivor outcomes, and market/faction feedback |
| Factions/escalation/war | PARTIALLY_IMPLEMENTED | `tribalmind.js` and social systems are wired into simulation | Verify intelligence vs rumor and actual escalation/retaliation consequences |
| Crime/justice/legitimacy/migration | PARTIALLY_IMPLEMENTED | `justice.js` computes justice access, legitimacy, grievance, and migration pressure; `Simulation.resolveJustice()` is wired and `tests/simulation-social-loop.test.js` passes | Add persistent population/faction state and crime/reporting/migration execution loop |
| Neural fear | PARTIALLY_IMPLEMENTED | `NeuralFearSystem` is constructed by Simulation and has unit tests | Establish whether advisory/predictive output can affect authoritative state |
| MASAC | PARTIALLY_IMPLEMENTED | `MASACIntegration` is constructed, disabled by default, and has pre/post hooks | Verify training validity, action validation and enabled runtime behavior |
| Orphan audit | PARTIALLY_IMPLEMENTED | Docs identify several modules without app-side importers | Complete importer/dynamic/test/UI audit and assign WIRE/MERGE/RETAIN_OPTIONAL/REMOVE/ARCHIVE |
| Advisory safety gate | UNKNOWN | No complete LLM proposal path identified in baseline | If in scope, enforce proposal → validator → planner → executor and test mutation isolation |
| Provenance/docs | PARTIALLY_IMPLEMENTED | `docs/PROVENANCE.md`, `ARCHITECTURE.md`, `BASELINE.md`, decisions exist | Add implementation evidence for each verified causal chain; database path/schema unavailable |
| Knowledge database | BLOCKED_EXTERNAL | `fear_ai_knowledge.db` not present in repository search; schema/path unavailable | Record exact external location/schema before any write; do not fabricate database updates |
| Closed-world integration scenario | IMPLEMENTED_AND_VERIFIED | `closed-world.js` covers 2 towns, 3 roads, bandit attack, survivor evidence, public rumor, perceived-risk rerouting, market delivery, faction reassessment, retaliation, cross-tick consequences via `tickClosedWorld`, cross-tick market price response, justice + report interactions, invasion-time retaliation with a faction resource pool (EVID-2026-08-27-CLOSED-WORLD-TICK-REDUCER), the all-systems integration test in `closed-world-all-systems.test.js` (EVID-2026-08-27-CLOSED-WORLD-ALL-SYSTEMS), the multi-good market where each town declares a `consumes` map covering every good (EVID-2026-08-27-MULTI-GOOD-MARKET), the audit fixes (EVID-2026-08-27-CLOSED-WORLD-AUDIT-FIXES), the configurable fear/grievance half-lives plus the full stock-flow economy with production, consumption, capacity, and spoilage (EVID-2026-08-27-FEAR-HALFLIFE-AND-TOWN-PRODUCES), the long-horizon P0 fixes that broke the death-spiral, structural infeasibility, and chronic-saturation failure modes (EVID-2026-08-27-LONG-HORIZON-P0-FIXES), the determinism contract on `HysteresisController` (EVID-2026-08-27-HYSTERESIS-DETERMINISM), and the per-faction raid cooldown that produced a realistic ~19% raid rate over 200 ticks (EVID-2026-08-27-RAID-COOLDOWN). The 200-tick quantitative trace in `scratchpad/long_horizon_cooldown.mjs` confirms the dynamic equilibrium. | Add per-target `memoryOfLoss`, multi-target raids, scale the cooldown by escalation level, and promote to `RUNTIME_VERIFIED` |
| Final independent audit | PARTIALLY_IMPLEMENTED | `docs/MODULE_AUDIT.md` records evidence-based verdicts: RETAIN_OPTIONAL for modules with tests, PRODUCTION_OWNER for the two systems actually wired into `Simulation` (`fearcore.js`, `memorysystem.js`), and explicit REACH-AUDIT findings that 6+ "Phase 3" modules are imported only by `tests/phase3.test.js` and have no production wiring. Production placeholders in `agentactions.js` (`nearObstacle`), `featureengineer.js` (population/trait features), `masac_worker.js` (`trainStep`), and the Rust lane (`generate_random_numbers`) have been replaced; the in-tree Tauri build was repaired and `cargo test rng_tests` is now green; the silent `Math.random()` non-determinism in `hysteresis.js` is fixed via a constructor-injected `rng`. The world-completion directive's §2 machine-readable domain-maturity map is now at `docs/DOMAIN_MATURITY.md` and records 44 domains against the full evidence ladder plus the breadth-debt ranking and heuristic register. | Either wire the Phase 3 modules into the production `Simulation` runtime, or move them to `tests/_fixtures/`. The reach-audit findings are flagged in `MODULE_AUDIT.md` for the next slice. |

## Latest evidence

- 2026-08-29: co-adaptive trade / banditry / security / information campaign, Movement 2 (EVID-2026-08-29-CO-ADAPTIVE-TRADE-CAMPAIGN). Built a persistent **Two Roads benchmark world** (`two-roads-world.js`): two settlements (origin, destination), two viable routes (road-a: short/cheap/dangerous; road-b: long/costly/safer), 4 merchants with heterogeneous risk-tolerance / switching-cost / cargo-value-sensitivity / route-familiarity / information-confidence / per-route beliefs, a bandit with recency-weighted per-route traffic-and-cargo beliefs and payoff-driven relocation, a finite patrol with single-route coverage + travel cost + detection / interception rates, an exposure ledger (merchantExposureTicks, merchantExposureDistance, exposedCargoValue, eligibleAmbushOpportunities, banditContactWindows, patrolCoveredExposure), and structured `MERCHANT_ROUTE_DECISION` / `BANDIT_RELOCATION` / `PATROL_INTERCEPTION` / `CARGO_LOSS` / `PATROL_REDEPLOY_COMPLETE` events. Beliefs are first-class (no omniscience — merchants carry their own per-route `beliefs` map with `perceivedDanger`, `confidence`, `sourceType`, `tick`, and confidence decays ~5% per tick). Switching cost enforced via `lastRoute` + `lastRouteSwitchTick` + `switchingCost` ticks. Cargo loss routes through the destination market's `deliverCargo` so a real `CARGO_LOSS` event reduces the destination's inventory and amplifies shortage-driven price (Movement 2 §34/§35/§37). 30 new tests across `tests/two-roads-world.test.js` (15), `tests/two-roads-metamorphic.test.js` (9), and `tests/two-roads-cat-and-mouse.test.js` (2) cover scaffolding, belief/perception separation, heterogeneous route choice, exposure ledger + ambiguous-zero, structured events, bandit belief/relocation, cargo→market, determinism, and the metamorphic + phase experiments (higher belief -> fewer A choices; higher risk-tolerance -> >= A choices; higher switching cost -> <= switches; stale belief until firsthand attack; credible attack updates belief; Phase 1 no-bandit baseline; Phase 2 bandit pressure; Phase 3 patrol displacement). Plus a generic **claim-anchored evidence receipt helper** (`evidence/receipt.mjs` + `evidence/seed-movement-2.mjs`) that captures (claimId, testFiles, sourceFiles via import closure, commandReceipt with exit code + output digest, fingerprint via the same scheme as the linter) and writes ADMISSIBLE evidence rows. The fingerprint scheme mismatch between `receipt.mjs` and `fingerprint.mjs` was discovered and fixed (both now use `computeSourceFingerprint`). The linter's `deriveFingerprintFiles` was extended to read from `row.sourceState.fingerprintFiles` and `row.transitiveDependencies` so receipt-helper rows are recognized. Final linter output: 63 evidence rows, 0 active contradictions, 0 errors. Maturity: trade `UNIT_VERIFIED` (4/5 ADMISSIBLE), merchants `LIVE_PATH_INTEGRATED` (5/5), bandits `UNIT_VERIFIED` (3/3), patrol `UNIT_VERIFIED` (2/2), market `CODE_EXISTS` (2/2), territory unchanged at `CONSEQUENCE_VERIFIED`, relationships unchanged at `LIVE_PATH_INTEGRATED`. Full verification reached 114 suites / 1042 tests (up from 110 / 1004); Vite build clean; `npm run lint:evidence` exits 0.
- 2026-08-28: evidence-status linter (EVID-2026-08-28-EVIDENCE-STATUS-LINTER, Movement 1 of the proof-gated campaign). The maturity table is now **derived** from a machine-readable evidence ledger. The new `evidence/` module provides the source-state fingerprint (`evidence/fingerprint.mjs`, sha256 over file contents + git HEAD + dirty flag), the maturity gate (`evidence/maturity.mjs`, walks an explicit promotion ladder: CODE_EXISTS → UNIT_VERIFIED → LIVE_PATH_INTEGRATED → CONSEQUENCE_VERIFIED → INTEGRATION_VERIFIED → CROSS_DOMAIN_INTEGRATED → DETERMINISM_VERIFIED → LONG_HORIZON_VERIFIED → STATISTICALLY_VERIFIED → RUNTIME_VERIFIED), the audit runner (`evidence/lint.mjs`, reports FRESH / STALE / BLOCKED / CONTRADICTED / INCOMPLETE / ADMISSIBLE per row), the migration script (`evidence/migrate.mjs`, seeds one SPECIFIED row per existing domain from the maturity table), and the test-change classifier (`evidence/maturity.mjs#classifyTestChange`). The territory domain has 6 real evidence rows (CODE_EXISTS, UNIT_VERIFIED, LIVE_PRODUCER, LIVE_CONSUMER, CONSEQUENCE_VERIFIED, CROSS_DOMAIN_INTEGRATED), all FRESH, no contradictions; the gate derives `CONSEQUENCE_VERIFIED`. 24 other domains have only the migrated SPECIFIED row and are therefore under-evidenced. 15 new tests in `tests/evidence-linter.test.js` (covering: SPECIFIED derivation, UNIT_VERIFIED derivation, LIVE_PATH_INTEGRATED derivation, STATISTICALLY_VERIFIED with `precisionMet: false` is NOT derived, HIGH-severity contradiction caps to CODE_EXISTS, MEDIUM-severity contradiction caps to UNIT_VERIFIED, fingerprint STALE on file change, fingerprint FRESH on no change, ledger read/write/append round-trip, test-change classifier for toEqual→toMatchObject, setXxxFrom migration, severity bump, audit end-to-end on a fixture ledger, two contradiction-cap cases). 5 test-change records in `docs/evidence/TEST_CHANGES.jsonl` (retrospective review of the territory slice's test edits, all classified as `TEST_DEFECT` or `SPECIFICATION_DEFECT`, none weakening the underlying contract). New `package.json` scripts: `lint:evidence` (audit runner), `evidence:migrate` (one-shot migrator). Full verification reached 110 suites / 1004 tests; Vite build clean; `npm run lint:evidence` exits 0 with 0 errors. The breadth-debt #1 (evidence-status linter) item is resolved; the next unblocked slice is Movement 2 (merchant risk + eligible exposure).
- 2026-08-28: TERRITORY vertical slice (EVID-2026-08-28-TERRITORY-VERTICAL-SLICE). The audit's debt #1 (territory) and debt #3 (per-perspective components) are both resolved. Town territory fields exist (`controlledBy`, `homeRadius`, `claimedRadius`, `contestedRadius`, `scarceResources`) and are set in `createClosedWorldScenario`. The new `canObserveTerritory(observer, intruder, world)` predicate is the legal observation path for territorial intrusions (replaces the ad-hoc trespass detection). The territory pass in `tickClosedWorld` iterates `allIntruders(world)` and writes directional `INTRUSION` events into the world event log plus per-pair `recordIntrusion` records into the observer's perspective of the directed relationship state. `FactionRelationshipVector` gained per-perspective `directedTerritorialPressure`, `directedGrievance`, `directedFear` maps with `getXFrom` / `setXFrom` accessors; the legacy fields are derived getters (mean across perspectives) with throwing setters. The `recordIntrusion` writer scales severity by `groupSize`, `armedStatus`, `scarceResourceOccupancy`, `priorIncidents`, and `duration` (the contextual severity table). `chooseStance` consumes `perceivedGroupSize` (capability gate) and `previousIncidentsCount` (trust-dampening: 5 prior incidents caps the damping at 0.2× baseline — the contextual-dampening upgrade). `pressureFrom(fromFactionId)` now uses the directed components, so the live `chooseStance` call site is fully directional. The Part XIV acceptance chain is end-to-end in the live path: territory state exists → intrusion is observed via the legal path → directional pressure changes → stance reasoning consumes it → structured explanation is emitted → invasion action selection respects the new gate → consequences persist. 13 new tests in `tests/territory-vertical-slice.test.js` prove the full chain. The `territory` row moved from `UNIT_VERIFIED` to `LIVE_PATH_INTEGRATED (with directional + contextual pressure)`; the `relationships` row moved to `CROSS_DOMAIN_INTEGRATED (with full directional pressure components)`. Full verification reached 109 suites / 989 tests (up from 108 / 976); Vite build passed. The breadth-debt #1 (territory) and #5 (per-perspective components) items are resolved; #1 is now the evidence-status linter.
- 2026-08-28: live perspective-aware `chooseStance` wiring (EVID-2026-08-28-PERSPECTIVE-AWARE-CHOOSE-STANCE-LIVE). The closed-world reducer now consumes `chooseStance` per evaluator perspective and emits a structured `STANCE_TRANSITION` event with `reason` / `evidence` / `capability` / `blocked` / `evaluatorId` / `militaryResources` / `informationConfidence` fields. A→B and B→A produce independent STANCE_TRANSITION events because the reducer reads `pair.pressureFrom(fromFactionId)` and `pair.getTrustFrom(fromFactionId)`, not the symmetric mean. The audit's P1 #1 and P1 #2 acceptance tests now run in the live production path, not just unit tests. New `tests/live-perspective-aware-choose-stance.test.js` (7 tests) proves the structured producer, the directional independence, the capability gate, the evidence gate, the backwards compatibility, the determinism, and the save/load round-trip end-to-end. `FactionDecisionModel` gained an `informationConfidence` constructor field (default 1.0). The `factions` maturity row moved from `LIVE_PATH_INTEGRATED (with stance machine)` to `LIVE_PATH_INTEGRATED (with structured live chooseStance)`; the `relationships` row moved from `LIVE_PATH_INTEGRATED (with directed trust + read-only legacy)` to `CROSS_DOMAIN_INTEGRATED (with perspective-aware live chooseStance)`. Full verification reached 108 suites / 976 tests; Vite build passed; production audit reported 0 vulnerabilities. The breadth-debt ledger's #1 (factions) and #2 (perspective-aware stance) items are resolved; #1 is now territory.
- 2026-08-26: targeted integration/FearCore verification passed: 46 tests.
- 2026-08-26: evidence/belief verification passed: 53 focused tests and 546 tests in the full suite; syntax and build passed.
- 2026-08-26: routing/economy verification passed: 11 focused tests and 553 tests in the full suite; syntax and build passed.
- 2026-08-26: town/merchant trade verification passed: 14 focused tests and 556 tests in the full suite; syntax and build passed.
- 2026-08-26: concrete Simulation trade wiring and persistent-trip verification passed: 6 focused tests and 559 tests in the full suite; syntax and build passed.
- 2026-08-27: repaired the merchant lifecycle contract; focused trade/routing verification passed (16 tests), full suite passed (18 suites, 565 tests), Vite build passed with existing chunk/import warnings, and production audit reported 0 vulnerabilities. Persistent graph ticks now preserve the real town map and deliver only at the final edge; direct immediate compatibility remains covered.
- 2026-08-27: added the first opt-in closed-world runtime slice in `closed-world.js`; focused closed-world tests passed (4 tests), covering bandit cargo loss, survivor evidence, public rumor derivation, and perceived-danger rerouting through concrete Simulation wiring. This remains partial and does not prove the full north-star causal chain.
- 2026-08-26: full baseline verification passed: 13 suites, 540 tests; build passed; production audit reported 0 vulnerabilities.
- 2026-08-26: no `fear_ai_knowledge.db` or SQLite database was found in the repository tree, so no database write was attempted.
- 2026-08-27: added validated character interactions (`interactions.js`) and justice feedback (`justice.js`); focused tests passed and the full suite reached 24 suites/576 tests.
- 2026-08-27: added deterministic convoy formation, escort assignment, ambush loss, and bandit adaptation in `convoy.js`; focused convoy/closed-world verification initially exposed and then corrected the causal-event expectation.
- 2026-08-27: connected closed-world attack output to the destination `Market`; disrupted cargo is removed from the merchant and surviving cargo is delivered through the market API. Full verification reached 27 suites/580 tests; build passed and audit reported 0 vulnerabilities.
- 2026-08-27: added deterministic bandit relocation and authorized faction retaliation in `escalation.js`; the closed-world chain now records faction action and bandit relocation. Full verification reached 28 suites/582 tests; build passed and audit reported 0 vulnerabilities.
- 2026-08-27: completed a source/import/test/build suspicious-module audit in `docs/MODULE_AUDIT.md`; retained optional modules where current evidence supports preservation and documented their limitations.
- 2026-08-27: replaced the Rust `generate_random_numbers` stub with a seeded xorshift64* stream (`main.rs:113-192` + `RngState`), the `agentactions.js` `nearObstacle` heuristic with a tiered obstacle-aware lookup (EVID-2026-08-27-OBSTACLE-GATE), and the MASAC worker `trainStep` `Math.random()` losses with a deterministic buffer-based metric in `masac_metrics.js` (EVID-2026-08-27-MASAC-METRIC). Verification reached 31 suites/595 tests; build passed and audit reported 0 vulnerabilities.
- 2026-08-27: wired the new obstacle contract into the simulation: `Simulation` now maintains a per-frame `obstacleSpatialHash` and populates each agent's `visuals.obstacles` via `queryNearbyObstacles(x, y)`, so the `nearObstacle` GOAP gate fires from real obstacle data instead of the legacy neighbor heuristic. Verification reached 32 suites/600 tests; build passed and audit reported 0 vulnerabilities. The integration test that hand-rolls a `visuals` object still hits the legacy fallback path, which is by design and emits the documented one-time warning.
- 2026-08-27: repaired the in-tree Tauri 2.10 build path: removed the dead `fs:*` permissions from `capabilities/default.json`, copied the existing repo-root `app-icon.ico` into the expected `src-tauri/icons/` location, and patched `main.rs` for the `get_window` → `get_webview_window` API rename, a `Vec<&String>` → `Vec<String>` NLL fix, and a removed unused `serde` import. `cargo check` now finishes with no errors or warnings, and `cargo test rng_tests` runs the 4 Rust tests in-tree; the JS suite and build remain green.
- 2026-08-27: removed the dead `FactionState` class from `closed-world.js` and added a `tickClosedWorld` reducer plus `runClosedWorldForTicks` driver that accumulate `grievance`, bandit road changes, and merchant cargo across ticks. The reducer only emits a `FACTION_REASSESSMENT` event when escalation actually changes. Verification reached 33 suites/609 tests; Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-27: extended the reducer to drive each town's `Market` through `setDemand('food', population)` and `consume('food', population)`, emitting a `MARKET_TICK` event with the post-tick quote (suppressed when the quote is unchanged from the previous tick). The `tickHistory` snapshot now also records market prices. Verification reached 33 suites/614 tests; Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-27: extended the reducer to resolve justice per town (`JusticeSystem.resolve` on a reported crime, suppressed when there is no crime so the audit trail records actual responses rather than idle drift) and to file a `Report` interaction through `InteractionEngine.execute('Report', guard, bandit, ...)` on every tick where a guard and bandit exist. The faction step's `confirmedLoss` now uses an up-to-tick attack count (corrected from a `=== tick` filter that the failure-first review caught). The `tickHistory` snapshot now also records justice state and report count. Verification reached 33 suites/619 tests; Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-27: extended the reducer with an invasion step that calls `executeRetaliation(faction, bandit, { tick })` for every faction in `RAID` state with `resources > 0`, picking the first bandit on a road that touches the faction's home town. `FactionDecisionModel` now accepts `townId`, `resources`, and `maxResources` (backward-compatible defaults). Each successful raid consumes 1 resource; a refill pass regains 1 resource per tick for factions in HOLD/DEFENSIVE, so each raid has a real cost. Verification reached 33 suites/625 tests; Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-27: added the all-systems closed-world integration test (`closed-world-all-systems.test.js`) and wired `Simulation.runClosedWorldStep` to bump a per-simulation `closedWorldTick` counter, form the convoy on tick 1, and emit a `FACTION_ACTION` event when the south faction is in RAID state. The reducer's resource-refill cap was corrected from `max(1, ...)` to `max(0, ...)` so a faction with `maxResources: 0` stays at zero resources. Verification reached 34 suites/632 tests; Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-27: extended the closed-world market step to handle multiple goods. Each town now declares a `consumes` map (`{ kind: perCapitaDemand }`); the reducer iterates over every `(town, kind)` pair, runs `setDemand` + `consume` + `getQuote`, and emits a `MARKET_TICK` event per good per town. The change-detection key became `${townId}::${kind}` so a per-good quote change is observable while stable goods stay quiet. The default scenario seeds `consumes: { food: 1, tools: 0.2 }`. Verification reached 34 suites/633 tests; Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-27: closed-world audit fixes (EVID-2026-08-27-CLOSED-WORLD-AUDIT-FIXES). Split `planRetaliation` (pure) from `executeRetaliation` (mutating, with `executedActions` idempotency guard); routed both `runClosedWorldStep` and the reducer through the plan, and made the reducer the single executor. The `FACTION_ACTION` and `INVASION` events now share the same `actionId` / `causationId`, and the all-systems integration test asserts this strict causal order across 5 ticks. The reducer's grievance inputs were rewritten to use the current-tick *flow* (`newAttacksThisTick`) instead of cumulative `attacksUpToTick`, and a slow-moving `memoryOfLoss` state on `FactionDecisionModel` was added so historical trauma persists with multiplicative decay (default 5%/tick, ≈ 13.5-tick half-life, configurable via `memoryDecayPerTick`). The hardcoded `supplyShortage: 0.1` was replaced with a per-town derivation from the actual market quotes (mean shortage across consumed goods). The multi-good provenance row was corrected from "3 new tests" to "2 new tests, 3 existing tests updated" with a note on the +1 net delta from the dedup. Verification reached 34 suites/643 tests; Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-27: fear half-life and town production (EVID-2026-08-27-FEAR-HALFLIFE-AND-TOWN-PRODUCES). Added `decayFromHalfLife(halfLifeTicks) = 1 - 2^(-1/halfLifeTicks)` to `factioncore.js` and a new `advanceEmotion` method on `FactionDecisionModel` that runs the per-tick stock-flow update. Fear uses a leaky integrator so it tracks the stimulus but lingers after it drops; grievance and memoryOfLoss use additive-with-decay. Constructor accepts `fearHalfLifeTicks` (default 6.6, ≈ 10%/tick) and `griefHalfLifeTicks` (default 22.8, ≈ 3%/tick). Added `Market.setCapacity`, `setSpoilageRate`, `produce`, and `spoil` to `economy.js`; `deliverCargo` now respects the storage capacity. The reducer's market step now runs the full stock-flow loop per `(town, kind)`: produce → setDemand → consume → spoil → quote. Default scenario seeds each town with `produces`, `storageCapacity`, and `spoilageRate` (north: granary, 1.5 food/0.1 tools, 5% food spoil; south: smithy, 0.5 food/0.3 tools, 5% food spoil). Verification reached 34 suites/659 tests; Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-27: long-horizon P0 fixes (EVID-2026-08-27-LONG-HORIZON-P0-FIXES). Independent re-audit (long-horizon trace + quantitative check across baseline / nervous / norraid variants) found three P0 failures: (1) both factions saturated at `grievance=1.0, lastDecision=RAID, resources=0` from tick ~4 and stayed there for 200 ticks — a death-spiral caused by the refill rule `if (lastDecision === 'RAID') continue;` skipping empty factions; (2) south's food inventory permanently zero because `produces.food = 0.5 < consumes = 1.0`; (3) the `0.4` grief coefficient on supplyShortage meant chronic shortage alone saturated grievance. Fixed by rebalancing the grief coefficients (chronic 0.4→0.05, acute 0.2→0.4), adding a resource gate to `FactionDecisionModel.reassess` (`lastDecision = (esc >= RAIDING && hasResources) ? 'RAID' : 'HOLD'`), and raising south's food production to 1.2 for local viability. New regression tests in `tests/long-horizon-dynamics.test.js` cover the death-spiral, structural infeasibility, saturation, and diversity properties. At tick 200 the system is now: south at full peace (grief 0, res 2), north oscillating between res=0/RAID and res=1/HOLD with chronic grievance 0.83 from the tools shortage — a real dynamic equilibrium, not a degenerate state. Verification reached 35 suites/665 tests; Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-27: hysteresis determinism (EVID-2026-08-27-HYSTERESIS-DETERMINISM). Follow-up orphan audit found `hysteresis.js` called `Math.random()` at lines 109 and 138 (FREEZE rolls). The module is not in production (only `tests/phase3.test.js` imports it; the actual production fear-band is `fearcore.js` via `brain.js`), but the silent non-determinism was a contract violation against the deterministic-core doctrine. Fixed by adding a `{ rng = Math.random }` constructor option and replacing the two `Math.random()` calls with `this.rng()`. New `tests/hysteresis-determinism.test.js` (5 tests) proves the rng is wired, the FREEZE branch is reachable with low rng, the HIDE branch is taken with high rng, and the same seed produces identical state sequences across instances. Verification reached 36 suites/670 tests across 5 consecutive runs (no flakiness); Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-27: per-faction raid cooldown (EVID-2026-08-27-RAID-COOLDOWN). The long-horizon P0 fix (EVID-2026-08-27-LONG-HORIZON-P0-FIXES) broke the resource death-spiral but left a follow-on issue: north oscillated `res=0 ↔ res=1, dec=RAID ↔ HOLD` every other tick, raiding ~100 times in 200 ticks. Added `lastRaidTick` to `FactionDecisionModel` (default `null`), a `raidCooldown` option to `tickClosedWorld` (default 5 ticks), and a check in the invasion step that skips factions whose `tick - lastRaidTick < cooldown`. New `tests/raid-cooldown.test.js` (4 tests) proves the cooldown blocks the immediate next raids, allows raids after the cooldown elapses, and is bypassable for backward compat. Long-horizon trace confirms north's raid count dropped from ~100 to 38 in 200 ticks (19% rate), a realistic campaign frequency. Verification reached 37 suites/674 tests; Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-27: brain.js determinism + dual-ownership audit (EVID-2026-08-27-BRAIN-DETERMINISM, EVID-2026-08-27-BRAIN-DUAL-OWNERSHIP, EVID-2026-08-27-RUST-PARITY-AUDIT). The Relentless Autonomous Evolution charter's fresh-session audit found three P0 trust failures in the live production path. **(1)** `brain.js` called `Math.random()` in the constructor (10 trait slots), `reset()` (10 trait slots), and inside `decide()` at lines 172, 173, 421, 426, 429, 497, 498, 526, 527, 537, 538, 577, 578 — silent non-determinism that the prior EVID-2026-08-27-HYSTERESIS-DETERMINISM slice missed because that slice only audited the test-only `hysteresis.js`. Fixed by adding a `{ rng = Math.random }` constructor option to `Brain` and replacing all 9 `Math.random()` calls in `decide()`/`mutate()` with `this.rng()`. **(2)** The inline `Brain.decide()` state machine mutates `this.state` directly after `FearCore.update()` and resets `fearCore.state = this.state` at line 387 every tick, nullifying FearCore's panic-lock semantics. A scenario sweep found 15/35 scenarios (43%) produce `brain.state = 'AGGRESSIVE'` while `fearCore.state = 'CALM'` — a real divergence, not latent. Pinned as a separate finding with a regression test; the migration to FearCore-as-authoritative is parked as a separate slice per the master plan's compatibility rule. **(3)** `src-tauri/src/main.rs` (543 lines) contains zero references to `fear|panic|threshold|trauma|hysteresis|habituation` — the canonical Rust fear model referenced by `RUST_PARITY.md` does not exist in the current checkout. Updated `RUST_PARITY.md` to record the explicit search result so future P1 workers don't re-litigate the question. New regression tests in `tests/brain.test.js` (7 determinism tests + 1 dual-ownership test) prove the rng is wired, the same rng produces identical traits and movement, and the dual-ownership divergence is observable. Verification reached 37 suites/681 tests; 5/5 brain-suite flakiness runs consistent; Rust 4/4; build clean; audit 0 vulnerabilities.
- 2026-08-26: audit still finds placeholder markers and the build reports static/dynamic import overlap; these remain open.

## Completion rule

No row may be changed to `IMPLEMENTED_AND_VERIFIED` without implementation, production reachability, local tests, cross-system tests, observable consequence, documentation, and provenance evidence. This ledger intentionally does not claim the full north-star architecture is complete.
