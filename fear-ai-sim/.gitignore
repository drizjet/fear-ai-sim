node_modules/
coverage/
dist/
target/
test_output.txt
null
*.log
.DS_Store
__pycache__/
*.pyc
**/bin/
!/bin/
!/bin/fear-ai.js
obj/
# NOW-3: heavy-gate verdict snapshot refreshes every run; record lives in
# the evidence ledger milestones, not in a churning artifact.
evidence/heavy-suites.json
# Godot editor artifacts: the import cache is a build product and the .uid
# sidecars are regenerated by the editor on import. The in-engine conformance
# runs launch the real binary, so these appear locally but are not source.
**/.godot/
*.uid
