# Fear AI — Campaign Manager State

**Rule:** repository evidence outranks historical claims. Development verification is not supervisor acceptance.

## Current tree

- Repository: `fear-ai-sim/`; ESM JavaScript with Jest; orientation in `../README.md`.
- Current automated suite: **163 suites / 545 tests passing** (2026-09-23).
- Current acceptance: development-verified only; no supervisor admission claimed.
- Git repository initialized inside this checkout on 2026-09-22 (the quarantined empty root `.git` had been purged — see `ROOT-RESIDUE-NOTES.md`); source fingerprinting is unblocked — only Knowledge DB writeback remains `BLOCKED_EXTERNAL`.

## Completed responsibility

`RESP-OBSERVATION-RUMOR-LATENCY-DECAY-001` is development-verified.

Delayed rumor delivery now decays delivered confidence by in-transit latency beyond the nominal one-tick handoff (half-life 10 by default, per-item override), distortion compounds with latency, recipients resolve source trust locally (`trustFor` > reputation book > flat `sourceTrust`) with skepticism gating evidence weight, propagation runs through the relayer's local belief via `relayRumor`, and delivered rumor beliefs age with the world clock without rewriting estimates. Deterministic across identical worlds and save/load.

`RESP-TIME-OWNERSHIP-001` (rumor→belief world-clock wiring) is also development-verified: rumor publish, evidence, and arrival timestamps all come from the world clock, never `Date.now`.

`RESP-EVENT-CAUSALITY-001` is development-verified.

Belief paths now run through canonical event emission. `worldStep` queues and delivers rumors inside the turn as `RUMOR_QUEUE` → `RUMOR_DELIVER` → `RUMOR_DELIVERED` → `BELIEF_UPDATED` action events with ordered parentage to the TURN (timing unchanged: the clock advances before actions, so `deliveryTick` computes exactly as the former post-tick call). Belief decay is recorded on the TURN event (`beliefDecay`: route/rumor claim counts + factors) instead of mutating confidence silently. Decisions that consume beliefs — `TRADE_ROUTE_DECISION`, `ROAMING_GROUP_ROUTE_ENCOUNTER`, `QUEUE_AWARE_SETTLEMENT_ECONOMY` — cite `beliefProvenance` (producing event id/tick/type, always an earlier seq). `RUMOR_RELAY` relays through the relayer's local belief as a parented event. ROUTE_OBSERVATION's child `BELIEF_UPDATED` now carries the real `route:<id>:danger` claim (it had been `claim: undefined` because `observeRoute` never returns one). Deterministic across identical worlds and save/load.

`RESP-UTILITY-AFFORDANCE-RUNTIME-001` is development-verified.

`utilitycore.js` is the shared utility/affordance runtime: `AffordanceRegistry` (id-keyed, duplicate-rejecting, actor/target type gating, runtime register/unregister) owns affordance registration, and `UtilityRuntime` owns utility scoring — prerequisites hard-block, response-curve/weight considerations, clamped-mean finalScore, narrow-band selection through the injectable serializable RNG. `DecisionCore` and `InteractionCore` are thin facades with unchanged public API and RNG consumption order, so every production path (society DECISION/FACTION_EVALUATION/INTERACTION_EVALUATION, `InteractionCore.decide/validate`, `AdvisoryGate`) runs through one implementation, and registered affordances extend character interactions at runtime. Deterministic across identical seeds.

`RESP-CAUSAL-CHAIN-INSPECTOR-001` is development-verified.

`SocietyCore.causalChain(eventId)` walks any event's parent chain root-first while validating ordered parentage (parent.seq < child.seq, no cycles, no dangling parents), surfaces cited `beliefProvenance` producers with their own lineages, and never mutates history. It immediately surfaced real structure: `TRADE_ROUTE_DECISION` parents cross-tick to the actor's latest `ROUTE_OBSERVATION`, so a decision's lineage root is the tick where its belief was observed.

The two previously-UNKNOWN ledger rows are closed: the advisory gate is production-wired (`SocietyCore.advisory` → canonical `ADVISORY_VALIDATION` events, pure validation, no state mutation) and the module import scan found zero orphans with keep/wire/archive decisions recorded.

`RESP-CHARACTER-INTERACTION-AFFORDANCES-001` is development-verified.

`INTERACTION_EXECUTION` runs registered affordances end-to-end: `AdvisoryGate` is the only approval path, approved interactions apply the declared `INTERACTION_EFFECTS` table (transform/recruit convert the target's type), and rejections land as canonical TURN children with their blockers while mutating nothing. Actor/target resolution prefers world-registered actors so effects hit world state, and actor `type` now persists through save/load — the loop consequence (a converted target rejects repeat transforms at the type gate) survives the round-trip. The `Character interactions` ledger row and IMPLEMENTATION_STATUS item 6 are closed.

`RESP-EVENT-GRAPH-AUDIT-001` is development-verified.

`SocietyCore.auditEventGraph()` validates the entire history in one read-only pass: unique ids, contiguous seq, seq mirroring `eventSeq`, resolvable parents, and ordered parentage in both seq and tick (`parent.tick <= child.tick`) — seq ordering makes cycles structurally impossible, so verifying it rules them out. Healthy, empty, and deserialized worlds pass; every tamper class (dangling parent, order violation, tick regression, duplicate id, mirror drift) is detected and the world restores to clean.

Personality and Morale now have one canonical runtime owner: the `socialcore.js` classes, registered on the world (`setPersonality`/`setMorale`, world-RNG trait draws) and flowing into DECISION through `evaluationContext()` — `Personality.decisionBandWidth` owns the band formula when a class instance is present (plain-object fallback unchanged) — with `MORALE_UPDATE` emitting canonical `MORALE_SHIFT` TURN children and both revived as class instances across save/load. The `Personality` and `Morale` ledger rows are closed (brain.js is absent from this checkout; these classes are the sole owners).

`RESP-LEDGER-STALE-CLAIMS-AUDIT-001` is development-verified.

Every completion-ledger row citing files this checkout does not contain has been reconciled under the evidence rule. Eight rows — `Simulation/agents/combat` (which had claimed IMPLEMENTED_AND_VERIFIED), `FearCore live transitions`, `Brain scale cleanup`, `Habituation`, `Hysteresis`, `VR/biofeedback`, `Neural fear`, and `CI` — are retracted to the new `SOURCE_ABSENT` status with repo-wide glob evidence (0 matches for every cited source, no workflow file at either root, no FearBand rust anywhere in the workspace). `Reputation/trust`'s stale `socialdynamics.js` citation was corrected to the real `socialcore.js` `ReputationBook` (status stayed PARTIALLY_IMPLEMENTED until `RESP-REPUTATION-PUBLIC-PRIVATE-001` closed it on 2026-09-23 — the row is now `IMPLEMENTED_AND_VERIFIED`), and `Knowledge DB writeback` is recorded `BLOCKED_EXTERNAL` (no DB artifact in checkout). (Update: CI left `SOURCE_ABSENT` on 2026-09-22 when `.github/workflows/ci.yml` landed — seven SOURCE_ABSENT rows remained; six remained after the 2026-09-23 Habituation re-open; five after the same-day Hysteresis re-open; four after the neural-fear re-open; **two remain since the FearCore/Brain re-open** — every closure through the documented procedure (byte-exact extraction into `legacy/` + V8 integration), each row now `IMPLEMENTED_AND_VERIFIED`). A durable guard pins this: `tests/completion-ledger-integrity.test.js` fails any healthy row citing a missing file, any SOURCE_ABSENT row citing an existing one, any status outside the closed vocabulary, or any missing retraction.

`RESP-CRIME-JUSTICE-LEGITIMACY-LOOP-001` is development-verified.

The crime → report → justice → legitimacy → migration production loop runs through canonical event emission: `CRIME_COMMIT` resolves `resolveCrime` into a `CRIME_COMMITTED` event (commits iff the payoff is positive), `CRIME_REPORT` computes `reportCrime` and draws the outcome through the shared RNG into a `CRIME_REPORTED` event parented to the crime, `CRIME_JUSTICE` gates on a reported outcome, derives the remedy from `accessToJustice`, applies `updateLegitimacy` to the settlement authority, and emits `JUSTICE_RESOLUTION` (`authority: SETTLEMENT`, `remedy`/`solved`/`injustice`/`legitimacyBefore`/`legitimacyAfter`) parented to the report, and `CRIME_MIGRATION` runs `shouldMigrate` on the post-justice settlement legitimacy and emits `MIGRATION_EVALUATION` (`authority: SETTLEMENT`, `crimeId`/`justiceId`) parented to the justice event — every stage references and parents to its upstream event, so the whole cycle is one lineage from the TURN. A probability-0 report halts the cycle: justice throws `No reported crime` and nothing downstream is allocated. Accessible justice solves the case and stops the migration; inaccessible justice drops legitimacy below the migration threshold. Identical seeds replay the loop bit-for-bit, and the chain plus settlement legitimacy survive save/load with a post-restore stage parenting to the pre-restore justice event.

`RESP-INFO-DOC-CONSISTENCY-001` is development-verified.

The three orientation docs are machine-checked against each other and the repository: gate counts must agree across `README.md`, `CAMPAIGN_STATE.md`, and `FEAR_AI_GLOBAL_WORK_LEDGER.md` and the suite count must equal the test files on disk; every doc-map target must exist; every suite cited in the README must exist; and this file's next responsibility must equal the work ledger's selected responsibility. On first run it caught three phantom suite references in the new README (`scheduler-ownership.test.js`, `scheduler-dispatch.test.js`, `persistence-equivalence.test.js` — none exist in this checkout), which were corrected to real citations.

## Audit pass of 2026-09-22 (evidence-rule sweep)

A full source + docs + tests audit (all 8 modules, all 4 orientation docs, all 144 suites read against their claims) found and fixed five defects, each now pinned:

1. **Stale unguarded gate pointer** — `docs/IMPLEMENTATION_STATUS_2026-08-26.md` still declared "Current gate: 142 suites / 405 tests" while the real gate was 144/416; the doc-integrity check only covered three docs, so this drifted silently. Corrected to the live gate and that doc now joins the gate-agreement check (`tests/doc-integrity.test.js`).
2. **Wall-clock leakage in `socialcore.js`** — `BeliefEvidence`/`AgentBelief` fell back to `Date.now()` whenever `now` was not injected (contradicting RESP-TIME-OWNERSHIP-001 "never `Date.now`"), and `AgentBelief.decay()` was dead code (`decay(...)` had zero references — aging is owned by `SocietyCore.decayRouteBeliefs`/`decayRumorBeliefs`) whose default argument *was* `Date.now()`. Fallbacks now use `0` (world epoch), `decay()` is removed, and `tests/time-ownership.test.js` gained a source scan that fails if any production module references `Date.now(`, `Math.random(`, or `performance.now(`.
3. **Rumor auto-id namespace poisoning** — `RumorNetwork.publish` computed the auto id before checking the counter, so an explicit `{ id: 'rumor-1' }` published first made the *next* auto publish throw `Duplicate rumor id` (own counter emitting collisions). Auto ids now skip explicitly reserved ids; pinned in `tests/rumor-identity.test.js`.
4. **`createTrip` duplicate-id guard masked by the stock check** — the duplicate check ran *after* `stock < amount → null`, so a duplicate trip id against insufficient stock returned a business `null` instead of surfacing the invariant violation. Guard reordered first; pinned in `tests/market-conservation.test.js`.
5. **`confidenceHalfLife` lost across save/load** — `serialize()` saved `maxRumors`/`maxQueue` but not `confidenceHalfLife`, so a restored world silently reverted to the default half-life of 10 (delivery-decay semantics changed post-restore). Now serialized/deserialized; pinned in `tests/observation-rumor-latency-decay.test.js` (round-trip + delivery at the restored rate).

Plus one coverage gap closed: the `recruit` entry in `INTERACTION_EFFECTS` had **no execution test** (docs claimed "transform/recruit convert the target type" but only transform was ever executed). A new case in `tests/character-interaction-execution.test.js` registers `recruit` at runtime and drives it end-to-end through the advisory gate to conversion.

Ruled out during the audit (checked, not defects): every suite cited in README/CAMPAIGN/ledger exists on disk; `completion-ledger.md` citations all resolve (machine-guarded); `persistence-probe.txt` is exactly the 17 bytes the ledger claims; the `recruit`-not-in-default-catalog asymmetry is intentional (registered-at-runtime design, per `utility-affordance-runtime.test.js`); no `TODO/FIXME/HACK/debugger/console.log` anywhere in production; no orphan modules; `Math.random`/`Date.now` appear only in test mutants/comments after fix #2.

Gate after fixes: **144 suites / 421 tests passing** (5 new cases, 0 failures).

## Verification evidence

- Convoy/escort/bandit loop suite: `tests/convoy-escort-bandit.test.js` — 7/7 passing (full cycle with belief provenance + causal chain, bandit-success conservation, cross-tick save/load bit-for-bit continuation, stage-gate invariants incl. invariant-before-business error precedence, mid-loop worldStep deferral, determinism).
- CI workflow suite: `tests/ci-workflow.test.js` — 2/2 passing (workflow exists with `npm ci` + `npm test` on push/PR, Node version pinned, gate command matches `package.json`, README references the workflow; `fetch-depth: 0` pinned so remote runs re-resolve `origin/master` blobs for the reconciliation guard).
- Negative controls for RESP-CONVOY-ESCORT-BANDIT-LOOP-001: resolution status gate removed (stage-gate case killed), victory comparison inverted (full-cycle + bandit-conservation cases killed), generic-trip deferral removed (worldStep case killed) — all three mutants accounted for by distinct failures (4 failed / 3 survived-attributable), production restored and re-verified green.
- Autopilot controller (`.agents/fear-ai-autopilot.mjs`): attempted standalone run 2026-09-22 — `@codebuff/sdk` is not vendored in this checkout and `CODEBUFF_APP_ID` is unset, so the controller cannot self-execute outside the Codebuff runtime. Its DIRECTIVE was executed manually for this wave (evidence-first work units, focused tests, negative controls, durable state updates, certificate-only completion); the file stays `keep` per the module-scan decision.
- Git fingerprinting unblocked 2026-09-22: repository initialized inside `fear-ai-sim/` (branch `main`, `node_modules/` ignored), initial commit stages the full checkout (165 files) — source hashes fingerprintable at HEAD.
- Latency/decay/distortion/recipient-local suite: `tests/observation-rumor-latency-decay.test.js` — 11/11 passing.
- Negative controls: latency-decay mutant killed (3 failures), recipient-trust mutant killed (2 failures), raw-relay mutant killed (1 failure); production restored and re-verified.
- Hidden-truth audit: `tests/observation-hidden-truth-twin-audit.test.js` — 2/2 passing.
- Belief-path event causality suite: `tests/event-causality-belief-path.test.js` — 6/6 passing (rumor action parentage chain, TURN decay summary, provenance on TRADE/QUEUE decisions, evidence-gain invariant, determinism + save/load).
- Negative controls for RESP-EVENT-CAUSALITY-001: dropped `RUMOR_DELIVERED` parentage killed (1 failure), stripped `beliefProvenance` killed (2 failures), TURN decay summary removed killed (1 failure); production restored and re-verified after each.
- Utility/affordance runtime suite: `tests/utility-affordance-runtime.test.js` — 5/5 passing (registry gates, delegation parity, shared-RNG band selection, InteractionCore/AdvisoryGate wiring, society production paths).
- Causal-chain inspector suite: `tests/causal-chain-inspector.test.js` — 4/4 passing (trade lineage to roots, rumor producer lineage, parentage validation, read-only).
- Negative controls for this wave: registry type gate removed (2 failures), inverse response curve broken (1 failure), lineage order reversed (2 failures), advisory source tampered (1 failure) — all killed, production restored and re-verified after each.
- Import scan: 8 modules, 0 orphans; advisory-gate and orphan-module UNKNOWN rows closed with evidence.
- Interaction execution suite: `tests/character-interaction-execution.test.js` — 6/6 passing (full catalog, gate rejections without mutation, loop consequence, save/load, determinism, runtime-registered recruit effect).
- Event graph audit suite: `tests/event-graph-audit.test.js` — 5/5 passing (healthy/empty/deserialized pass; dangling, order, tick, duplicate, mirror violations detected with clean restore).
- Personality/Morale ownership suite: `tests/personality-morale-ownership.test.js` — 6/6 passing (band-formula ownership, seeded draws, DECISION wiring, MORALE_SHIFT, save/load revival, determinism).
- Negative controls for this wave: audit parent-seq check removed (1 failure), advisory approval gate bypassed (3 failures), DECISION personality/morale wiring dropped (1 failure), decisionBandWidth ownership dropped (1 failure) — all killed, production restored and re-verified after each.
- Ledger integrity suite: `tests/completion-ledger-integrity.test.js` — 3/3 passing (citation existence in both directions, closed status vocabulary, all eight retractions explicit). Negative control: phantom `fearcore.js` citation on the World-tick row killed (1 failure), ledger restored and re-verified.
- Crime/justice/legitimacy/migration suite: `tests/crime-justice-legitimacy-migration.test.js` — 7/7 passing (full cycle in one turn, causal lineage + whole-history audit, primitive math parity, justice-flips-migration, report-gate stop, cross-tick save/load + continuation, determinism).
- Negative controls for RESP-CRIME-JUSTICE-LEGITIMACY-LOOP-001: legitimacy write skipped (killed), `retaliationFear` dropped from the report math (killed), migration fed pre-justice legitimacy (killed), chain parentage dropped (killed) — production restored and re-verified after each.
- Orientation doc consistency suite: `tests/doc-integrity.test.js` — 4/4 passing (gate agreement across the four docs incl. the IMPLEMENTATION_STATUS `Current gate` pointer + suite count vs disk, doc-map targets, README suite refs, next-responsibility agreement); first run caught three phantom README suite citations, corrected to real citations. Negative controls: README gate number drifted (killed), doc-map target renamed (killed), next-responsibility ids disagreed (killed). The 2026-09-22 audit found the IMPLEMENTATION_STATUS pointer had drifted to 142/405 unguarded, so that doc now joins the gate-agreement check.
- Player→invasion chain suite: `tests/player-invasion-chain.test.js` — 5/5 passing (full TURN-rooted lineage, pressure thresholds, war-state gate, faction fear propagation, input guards, save/load continuation).
- Merchant economy loop suite: `tests/routing-trade-economy.test.js` — 5/5 passing (plan→ship→deliver→price-response lineage, exact two-market conservation, WAIT/REJECTED paths, input guards, save/load continuation).
- Autopilot runnable suite: `tests/autopilot-agent.test.js` — 3/3 passing; `.agents/fear-ai-autopilot.mjs` loads through the installed `@codebuff/sdk` 0.10.7 (`loadLocalAgents` clean, `validateAgents` success with 0 errors), its controller loop driven headlessly (sync `function*` co-style per the SDK's `isValidGeneratorFunction`).
- Negative controls for the 2026-09-23 wave: lethal double-pressure removed (killed), war-state gate removed (killed), war-loot credit removed (killed ×2), profitability gate removed (killed) — all four attributed to distinct pins, production restored and re-verified green.
- Remote CI: branch `main` pushed to `drizjet/fear-ai-sim` 2026-09-23 (the monorepo's `master` untouched — our checkout is its `fear-ai-sim/` subtree, pushed as a new root branch); first Actions run green — [run 35874405000](https://github.com/drizjet/fear-ai-sim/actions/runs/35874405000), `npm ci` + the full 149/443 gate on ubuntu-latest. The `CI` ledger row moves PARTIALLY_IMPLEMENTED → IMPLEMENTED_AND_VERIFIED.
- Reputation contract suite: `tests/reputation-public-private.test.js` — 6/6 passing (public blend math + chained events, per-observer private isolation, guards-before-mutation, production DECISION consumption, both channels round-tripping save/load, seeded determinism).
- Negative controls for RESP-REPUTATION-PUBLIC-PRIVATE-001: private channel folded into public (killed), subject chain dropped (killed), private channel dropped from serialize (killed, isolated 1/1), reputation wiring dropped from `evaluationContext` (killed) — all four attributed to distinct pins, production restored and re-verified green.
- Monorepo reconciliation recon (2026-09-23): `main` (our orphan root, CI green ×2) vs `master`'s legacy `fear-ai-sim/` subtree share only 4 files (`.gitignore`, `README.md`, `package.json`, `package-lock.json`); `societycore.js` never existed upstream; the legacy tree holds at least `brain.js` and `biofeedback.js` — the `SOURCE_ABSENT` sources. Measured facts + four safe options (nothing destructive done) in `docs/MONOREPO_RECONCILIATION.md`.
- Autopilot live attempt (2026-09-23): `tools/run-autopilot-step.mjs` drove the controller over the real runner protocol — load → validate → auth → `run_terminal_command` certificate probe executed on the platform → `STEP_TEXT` dispatched → `POST api.codebuff.com/api/v1/chat/completions` (model `mimo-v2-flash`) returned **402 Payment Required**. Protocol complete end to end; remaining blocker is account credits (`AUTOPILOT_MODEL` override available for a cheaper model). Retry with `AUTOPILOT_MODEL=claude-3-5-haiku-20241022` (2026-09-23) returned the same 402 with the explicit platform message `Out of credits. Please add credits at https://www.codebuff.com/usage.` — the blocker is confirmed **account-level and model-independent**.
- SOURCE_ABSENT reconciliation (2026-09-23, `RESP-SOURCE-ABSENT-RECONCILIATION-001`): all seven rows verified against `origin/master:fear-ai-sim/` — 11 of 12 cited paths located (blob + sha256 pinned), `tests/fearcore.test.js` in neither tree, FearBand Rust 0 upstream paths — each row dispositioned (supersede / open-by-absence / deferred) in `docs/SOURCE_ABSENT_RECONCILIATION.md`; nothing extracted at that point (the guard is the tripwire — Habituation and then Hysteresis were re-opened later the same day, see below); `tests/source-absent-reconciliation.test.js` 4/4; CI now fetches `fetch-depth: 0` so remote runs re-resolve the legacy blobs.
- Monorepo option 1 executed (2026-09-23): default branch flipped `master` → `main` via `gh repo edit --default-branch main` — a reversible repo setting; no force-push, no push to `master`, no history rewrite (`docs/MONOREPO_RECONCILIATION.md` updated with the executed status).
- Raid contract suite: `tests/faction-raid-loop.test.js` — 7/7 (full TURN → evaluation → dispatch → resolution lineage with `raidUtility` scoring and `escalationLevel` surfaced on every stage, DEESCALATE business rejection, stage gates + guard order before mutation, defeat path, mixed-outcome loot conservation, save/load bit-for-bit continuation, seeded determinism).
- Habituation re-open executed (2026-09-23): `legacy/habituation.js` byte-exact (sha256 `df02134b…`, provenance in `legacy/PROVENANCE.md`, `-text` in `.gitattributes`), `HabituationBook` (socialcore) consumed by `FEAR_EVENT_RAISED` with canonical `FEAR_HABITUATED` events per exposure; row `SOURCE_ABSENT → IMPLEMENTED_AND_VERIFIED` with manifest + both guards updated through the designed tripwire; `tests/habituation-reopen.test.js` 7/7.
- Negative controls for both contracts: **7/7 mutants killed with per-mutant isolation runs** — raid stage gate removed, victory inverted, loot credit skipped, unattenuated fear gain, `FEAR_HABITUATED` parentage dropped, exposure book dropped from serialize, novelty rule disabled — each attributed to distinct assertions, production restored and re-verified green.
- Evaluation→raid chain (2026-09-23, `RESP-FACTION-EVALUATION-RAID-CHAIN-001`): `FACTION_EVALUATION`'s RAID selection now dispatches through the raid loop inside the same action — `FACTION_RAID_EVALUATION` (scored from the same context DecisionCore chose from) → `FACTION_RAID_DISPATCH` (context `force`/`bagSize` honored, confidence/stash defaults) → `FACTION_RAID_RESOLUTION` (one world-RNG draw, exact loot conservation) as one TURN-rooted lineage; DEESCALATE stops at a REJECTED dispatch with no resolution; unregistered/self targets record `raidChain` skip markers instead of throwing (existing clan-vs-rival scripts unchanged); non-RAID selections byte-identical; covered through the production `worldStep` entry; save/load bit-for-bit continuation; `tests/faction-evaluation-raid-chain.test.js` 8/8.
- Negative controls for the chain: chain never executes (5 failures), resolution stage dropped (4 failures), reference guard removed (killed — chain-suite skip test plus the legacy `event-causality-belief-path` suite both catch the throw), context overrides ignored (1 failure) — 4/4 killed by isolated per-mutant runs, production restored and re-verified green.
- Hysteresis re-open executed (2026-09-23, second of the day): `legacy/hysteresis.js` byte-exact (sha256 `40e5cb26…`, blob `208cbffc…`, provenance in `legacy/PROVENANCE.md`), `HysteresisBook` (socialcore) consumed by `FEAR_EVENT_RAISED` — asymmetric enter/exit gap (.15/.55 etc. verbatim), minimum-duration gate (SocietyCore passes 2), seeded FREEZE roll behind low morale, world-time records — with canonical `FEAR_STATE_TRANSITION` events per real transition; row `SOURCE_ABSENT → IMPLEMENTED_AND_VERIFIED` through the designed tripwire (manifest 11→10, both guards' row sets 6→5 at that step); `tests/hysteresis-reopen.test.js` 8/8.
- Autonomous faction macro tick (2026-09-23, `RESP-FACTION-AUTONOMOUS-TICK-001`): `FACTION_MACRO_TICK` action gives every registered faction a turn chained TURN-rooted through `FACTION_EVALUATION` (richest-other target resolved at turn time so later turns see earlier turns' loot moves, stable-sort insertion-order tie breaks, own-state numeric context), lone/empty worlds return the macro event alone, production entry `worldStep({ factionTurns })` default-off; `tests/faction-macro-tick.test.js` 7/7.
- Negative controls for the hysteresis re-open and the macro tick: **7/7 mutants killed with per-mutant isolation runs** — asymmetric gap collapsed (2 failures: gap + production transitions), minimum-duration gate removed (1: gate), FREEZE roll disabled (1: FREEZE), hysteresis dropped from deserialize (1: save/load), first-other target instead of richest (1: richest), first faction's turn skipped (5: every-faction/richest/peaceful/worldStep/macro-save-load), `worldStep` macro entry removed (1: worldStep) — each attributed to its distinct assertion, production restored and re-verified green.
- Retaliation loop (2026-09-23, `RESP-FACTION-RETALIATION-LOOP-001`): a raid `FACTION_RAID_RESOLUTION` now arms its victim — `grievanceBefore`/`grievanceAfter`, `retaliationThreshold`, `retaliationDepth` and `retaliationEligible` are recorded on every resolution, and an eligible victim runs its OWN production `FACTION_EVALUATION` against the attacker (the legacy macro utility's first-class `revengeValue` term plus the grievance folded into the two considerations the production RAID candidate actually reads) → `FACTION_RAID_EVALUATION` → `FACTION_RAID_DISPATCH` → resolution as one unbroken 8-node lineage rooted at the turn that resolved the raid; the counter resolves at depth 0 so retaliation cannot recurse, a barely aggrieved victim may still decline (eligibility is a licence, not a command), and the default threshold .5 leaves every existing world untouched; `tests/faction-retaliation-loop.test.js` 8/8.
- Rumors → reputation evidence (2026-09-23, `RESP-RUMOR-REPUTATION-EVIDENCE-001`): a delivered `reputation:` claim now judges its subject from the evidence that ARRIVED — verdict = the estimate the recipient ended up believing (clamped to [0,1]), weight = the confidence that survived latency, distortion and source trust — moving BOTH world-public standing and the recipient's private opinion as canonical `REPUTATION_UPDATE` events chained to the delivery that carried them; a weightless delivery emits nothing and claims outside the namespace are untouched; `tests/rumor-reputation-evidence.test.js` 6/6. Closes the Rumors row's remaining item.
- Neural fear re-open (2026-09-23, `RESP-NEURAL-FEAR-REOPEN-001`, third re-open of the day): `legacy/neuralfear.js` (sha256 `9b397180…`) and `legacy/neuralnet.js` (sha256 `9218cbde…`) extracted byte-exact with provenance; `NeuralFearModel` (socialcore) keeps the legacy semantics — ReLU hidden ladder + sigmoid output in [0,1], Xavier init, online gradient descent against the faction's ACTUAL fear, dropout, patience early stopping, running Welford normalization — with three documented repository-rule deviations (world RNG, LAZY initialization so constructing a world consumes zero draws, world-time history) and production wiring `NEURAL_FEAR_PREDICT`/`NEURAL_FEAR_LEARN`; row `SOURCE_ABSENT → IMPLEMENTED_AND_VERIFIED` through the designed tripwire (manifest 10→8 entries, both guards' row sets 5→4); `tests/neural-fear-reopen.test.js` 8/8.
- Three production defects found by grounding this wave and fixed: (1) the fear machine's RNG adapter was passed the RNG **source object** instead of a draw, so the seeded FREEZE roll would have thrown `TypeError` the first time it fired — both the Hysteresis and the new Neural wiring now draw through `this.random()`; (2) production never supplied morale to the fear machine, leaving the legacy FREEZE branch unreachable — the driver now passes the faction's assigned `Morale` (worlds that assign none keep the unfrozen path); (3) `blendReputation` divided by `max(1, weight)`, so a sub-unit-weight judgment pushed standing AWAY from its own value (a favorable rumor could lower standing) — a first judgment is now anchored at neutral .5 against the weight it does not carry, bit-identical for every previously pinned case (full-weight judgments, accumulated-weight blends) and pinned by the new rumor suite.
- Mutation-gate findings from that wave (both are test-quality defects the mutants exposed, not production defects): (1) the neural learning suite's loss-only assertion was **vacuous** — with the weight-update line zeroed the test still passed, because running feature normalization alone shifts the loss, so the suite now diffs the parameters themselves (floors of ≥20 weights and ≥5 biases moved, with the anatomy of the sparse sample documented) and that mutant dies; (2) one mutant escaped marker-based verification (`parent: summary` with no `MUTANT` marker left after a comment fix) — attribution is therefore by failure signature in an isolated run, never by a grep of markers.
- Negative controls for the three new contracts: **retaliation 4/4** (counter chain not dispatched, depth budget removed, threshold gate ignored, `retaliate: false` ignored), **neural 5/5** (eager initialization, inference drawing from the RNG, loss not improving, early stopping never firing, model dropped from serialize), **rumor evidence 5/5** (published estimate judged, weight not from arrived confidence, private channel skipped, namespace guard removed, delivery parent chain dropped) — each killed by an isolated per-mutant run, production restored and re-verified green.
- FearCore re-open (2026-09-23, `RESP-FEARCORE-REOPEN-001`, fourth re-open of the day): `legacy/fearcore.js` (blob `185494c8…`, sha256 in `legacy/PROVENANCE.md`) and `legacy/brain.js` (blob `163dfa7a…`) extracted byte-exact; the legacy 11-band contract is ported to `socialcore.js` with thresholds verbatim (ALERT .8 / ANXIOUS 1.4 / PANIC 3.8; exits .55/.8/1.2), panicLockTicks 10 behind the PRESENCE_BREAK bypass, the force-fallback for leaving an extended band, the snap-to-CALM guard and a bounded decisionTrace, plus the §332 scale adapter (`fearScale`: normalized 0..1 → raw 0..3.8) with the documented 0.21/0.37/1.0 threshold crossings; production drives it at the fear seam so a real band change is a canonical `FEARCORE_BAND_TRANSITION` chained off the exposure that produced the level it read (the hysteresis transition is staged first when both machines speak), consuming no RNG unless the contract's own PANIC rules fire. Three legacy quirks are preserved and documented rather than silently fixed (an unreachable CRAWLING-exit disjunct, HIDE-from-PANIC escaping before CRAWLING can trigger, and the §260 stay rule holding RECOVER); the row's cited legacy test was never committed in either tree and is recorded as a limitation, not recoverable. `tests/fearcore-reopen.test.js` 11/11 with 6/6 negative controls; closes both the `FearCore live transitions` and `Brain scale cleanup` rows.
- Grievance economy (2026-09-23, `RESP-FACTION-GRIEVANCE-ECONOMY-001`): being hurt now breeds resentment as well as fear — an applied fear gain adds `GRIEVANCE_PER_FEAR` (.5) of grievance and stamps the renewal tick, so the retaliation gate is reachable in ordinary worlds instead of only through scripted values; resentments RENEW on every gain (fear channel, raid resolutions, roaming shortages, injustices) and cool only while nothing renews them (5 idle world ticks, then .01/tick, floored at zero), with the TURN event recording how many resentments the pass cooled. Cooling disarms retaliation again, so the gate stays live rather than becoming a permanent scar; `tests/grievance-economy.test.js` 9/9 with 12/12 negative controls.
- Neural fear loop closed (2026-09-23, `RESP-NEURAL-FEAR-LOOP-001`): the extracted MLP stops being a read-only observer — `NEURAL_FEAR_FORECAST` lets a faction act on its own anticipation, but only a model that has EARNED trust (enough observed calibration samples AND an error inside tolerance) may act; the signed discrepancy `dread` between expected and felt fear moves the faction in bounded steps (blend × dread, capped by maxStep, inert inside a deadband), raising fear and narrowing the `opportunity` the production RAID candidate scores on — so a forecast demonstrably changes a real decision's input (RAID candidate .35 → .30) — while a reassuring forecast calms fear and restores the opening. Calibration is additive bookkeeping on the legacy model (post-update no-dropout error, EWMA) and round-trips save/load; `tests/neural-fear-loop.test.js` 10/10 with 8/8 negative controls.
- Assertion-quality guard (2026-09-23, `RESP-ASSERTION-QUALITY-GUARD-001`): the corpus audit found two suites whose negative controls passed for the WRONG reason — an optional-chained actual short-circuiting to `undefined` under `not.toBe(true)`, and a delivery control that never let the evicted item's own delay elapse — plus a name-vs-assertion mismatch (a "changes service" test that asserted no difference). All three were strengthened to prove what they claim, and a durable meta-test now fails any suite containing an unprovable signature (empty `toMatchObject`, literal `expect(true)`, a `?.` actual with a matcher `undefined` satisfies, an assertion inside `catch`, or an `it` with no assertion), self-verified against every banned signature; `tests/assertion-quality.test.js` 2/2.
- Full gate: **163/163 suites, 545/545 tests passing** (four suites added this wave — `tests/fearcore-reopen.test.js`, `tests/grievance-economy.test.js`, `tests/neural-fear-loop.test.js`, `tests/assertion-quality.test.js` — plus the FREEZE/adapter regression added to `tests/hysteresis-reopen.test.js`).
- Existing route, economy, material, persistence, and mutation gates remain green.

`RESP-CONVOY-ESCORT-BANDIT-LOOP-001` is development-verified.

The convoy/escort/bandit production loop runs through canonical event emission: `CONVOY_DISPATCH` (merchant's in-transit trip leaves under escort — invariant guards, then business rejection for unavailable routes) → `CONVOY_BANDIT_THREAT` (parent-chained to the dispatch) → `CONVOY_ESCORT_RESOLUTION` (one world-RNG draw: escortStrength + roll vs banditStrength; victory delivers, defeat robs) → `MARKET_TRIP_SETTLE` (the market consequence, chained off the draw). Convoy-owned trips are deferral-gated: the generic `progressPendingTrips` skips trips with an active ESCORTED/THREATENED convoy, so settlement happens only through the escort loop. Full cycle verified across save/load (bit-for-bit identical continuation), market balance conserved in both outcomes, and the whole history passes `auditEventGraph` + `causalChain`. Closes the ledger's `Convoys/escorts/bandits` row and covers the convoy/escort portion of IMPLEMENTATION_STATUS item 8.

`RESP-PLAYER-INVASION-CHAIN-001`, `RESP-ROUTING-TRADE-ECONOMY-LOOP-001`, and `RESP-REPUTATION-PUBLIC-PRIVATE-001` are development-verified (all closed 2026-09-23 — the last PROPOSED rows and the last open-gaps row).

The player→invasion chain runs `PLAYER_DAMAGE` (invariant guards precede any mutation; a lethal blow presses double war pressure) → `FEAR_EVENT_RAISED` (parent-chained to the wound; faction fear/threat perception updated) → `WAR_STATUS_UPDATE` (status computed from accumulated pressure against world thresholds, chained to the fear event) → `INVASION_RESOLVED` (gated behind WAR, one world-RNG draw, loot transfers exactly from settlement resources into war loot). One TURN-rooted lineage, seeded-identical save/load continuation — closes `Player-to-invasion chain` and the remaining half of IMPLEMENTATION_STATUS item 9.

The merchant economy loop runs `MERCHANT_ECONOMY_CYCLE` as `MERCHANT_ROUTE_PLAN` (route + profitability; WAIT stops the chain untouched) → `MARKET_TRIP_CREATE` (insufficient stock → REJECTED, no movement) → `MARKET_TRIP_SETTLE` → paired `MARKET_UPDATE` price responses (origin scarcity up, destination arrivals down), two-market stock total conserved exactly, seeded-identical save/load continuation — closes `Routing/trade/economy`.

`RESP-REPUTATION-PUBLIC-PRIVATE-001` closed the `Reputation/trust` row's remaining gaps: `REPUTATION_JUDGE` writes one of two isolated `ReputationBook` channels — PUBLIC world standing vs PRIVATE per-observer opinion (shared weighted-blend math, all invariants validated before mutation) — as canonical parent-chained `REPUTATION_UPDATE` events, and production `DECISION`s consume the book through `evaluationContext` (`reputation` + `reputationOf`), pinned end-to-end by score movement. Both channels round-trip save/load with seeded-identical continuation.

`RESP-SOURCE-ABSENT-RECONCILIATION-001`, `RESP-FACTION-RAID-LOOP-001`, `RESP-FACTION-EVALUATION-RAID-CHAIN-001`, `RESP-HYSTERESIS-REOPEN-001`, and `RESP-FACTION-AUTONOMOUS-TICK-001` are development-verified (all closed 2026-09-23), together with the Habituation re-open.

The SOURCE_ABSENT reconciliation verified every stale ledger row against `origin/master:fear-ai-sim/`, pinned each located source with blob id + sha256 in `docs/SOURCE_ABSENT_RECONCILIATION.md`, dispositioned each row, and left a guard (`tests/source-absent-reconciliation.test.js` 4/4) that re-resolves every blob on each gate run and forbids extraction outside the documented re-open procedure. The raid loop production-wired the once-orphaned `evaluateRaid`/`raidUtility`/`escalationLevel` layer as `FACTION_RAID_EVALUATION` → `FACTION_RAID_DISPATCH` → `FACTION_RAID_RESOLUTION` (`tests/faction-raid-loop.test.js` 7/7); the chain contract connects a production DecisionCore RAID choice to that loop inside one action (`tests/faction-evaluation-raid-chain.test.js` 8/8); Habituation and Hysteresis were re-opened byte-exact through the procedure with `HabituationBook`/`FEAR_HABITUATED` and `HysteresisBook`/`FEAR_STATE_TRANSITION` in production (`tests/habituation-reopen.test.js` 7/7, `tests/hysteresis-reopen.test.js` 8/8); and the autonomous macro tick gives every registered faction a TURN-rooted turn with no manual targeting (`tests/faction-macro-tick.test.js` 7/7). Negative controls for these waves: 7/7, 4/4, and 7/7 mutants killed by isolated runs, production restored and re-verified green each time.

`RESP-FACTION-RETALIATION-LOOP-001`, `RESP-RUMOR-REPUTATION-EVIDENCE-001`, and `RESP-NEURAL-FEAR-REOPEN-001` are development-verified (all closed 2026-09-23).

The retaliation loop closes the Factions row: a raid resolution arms its victim with recorded grievance/threshold/depth/eligibility, and an eligible victim evaluates and dispatches its own counter-raid against the attacker through the same production seam DecisionCore uses, chained off the resolution that struck it, with one level of budget bounding the loop (`tests/faction-retaliation-loop.test.js` 8/8). The rumor-evidence contract closes the Rumors row's last remaining item: delivered `reputation:` claims move public standing and the recipient's private opinion from the evidence that arrived, never the published intent (`tests/rumor-reputation-evidence.test.js` 6/6). The neural-fear re-open is the third execution of the documented re-open procedure: two legacy sources byte-exact with provenance, a production `NeuralFearModel` trained by world truth, and the row moved through the designed tripwire (`tests/neural-fear-reopen.test.js` 8/8). The same wave fixed three production defects (a latent FREEZE-roll `TypeError`, an unreachable FREEZE branch because morale was never supplied, and a reputation blend that moved sub-unit-weight standing away from its own value) with new regression pins.

## Next responsibility

`RESP-SIMULATION-AGENTS-COMBAT-REOPEN-001` — the fifth execution of the documented re-open procedure: `simulation.js`, `agent.js` and `learningagent.js` (blobs `2cc0c2dc…`, `f3815a84…`, `562faea5…`, pinned in `docs/SOURCE_ABSENT_RECONCILIATION.md`) extracted byte-exact with provenance, an actor/combat runtime integrated in production through the implement-and-test pass that row has always needed, and the `Simulation/agents/combat` row dispositioned honestly (the society world's actors cover it only partially; combat needs a pass of its own and is the last fear-AI row whose sources are locatable — VR/biofeedback waits on an explicit product-scope decision). Closed 2026-09-23: `RESP-FEARCORE-REOPEN-001` — the fourth re-open: `legacy/fearcore.js` (blob `185494c8…`) and `legacy/brain.js` (blob `163dfa7a…`) byte-exact, the 11-band contract plus its §332 scale adapter in production (a real band change is a canonical `FEARCORE_BAND_TRANSITION`), three legacy quirks preserved and documented rather than silently fixed, `tests/fearcore-reopen.test.js` 11/11 with 6/6 negative controls, closing both the `FearCore live transitions` and `Brain scale cleanup` rows; `RESP-FACTION-GRIEVANCE-ECONOMY-001` (hurt breeds resentment — fear feeds grievance, resentments renew on every gain and cool only while nothing renews them, so an ordinary world reaches the retaliation gate on its own and cools back below it — `tests/grievance-economy.test.js` 9/9 with 12/12); `RESP-NEURAL-FEAR-LOOP-001` (calibrated anticipation acts on fear and on the RAID decision's `opportunity`, bounded by blend/cap/deadband — `tests/neural-fear-loop.test.js` 10/10 with 8/8); `RESP-ASSERTION-QUALITY-GUARD-001` (durable assertion-quality guard, landed with the corpus audit — `tests/assertion-quality.test.js` 2/2). Earlier the same day: `RESP-FACTION-RETALIATION-LOOP-001` (the struck faction evaluates and runs its own counter-raid off the resolution that hit it — `tests/faction-retaliation-loop.test.js` 8/8), `RESP-RUMOR-REPUTATION-EVIDENCE-001` (delivered `reputation:` claims judge their subject from arrived evidence across both channels — `tests/rumor-reputation-evidence.test.js` 6/6) and `RESP-NEURAL-FEAR-REOPEN-001` (third re-open — `legacy/neuralfear.js` + `legacy/neuralnet.js` byte-exact, `NeuralFearModel` predicting/learning in production — `tests/neural-fear-reopen.test.js` 8/8), with three production defects fixed in the same wave (FREEZE-roll RNG adapter, unreachable FREEZE branch, sub-unit-weight reputation blend). Selection closed 2026-09-23: `RESP-HYSTERESIS-REOPEN-001` — second re-open of the day: `legacy/hysteresis.js` byte-exact (blob `208cbffc…`, sha256 `40e5cb26…` pinned in `docs/SOURCE_ABSENT_RECONCILIATION.md`), `HysteresisBook` (asymmetric enter/exit gap, minimum-duration gate, seeded FREEZE roll, world-time records) consumed by `FEAR_EVENT_RAISED` with canonical `FEAR_STATE_TRANSITION` events (`tests/hysteresis-reopen.test.js` 8/8), row `SOURCE_ABSENT → IMPLEMENTED_AND_VERIFIED` through the designed tripwire (manifest 11→10 entries, both guards' row sets 6→5 at that step); and `RESP-FACTION-AUTONOMOUS-TICK-001` — `FACTION_MACRO_TICK` gives every registered faction a turn inside one TURN-rooted lineage, richest-other targets resolved at turn time, production entry `worldStep({ factionTurns })` (`tests/faction-macro-tick.test.js` 7/7), after `RESP-FACTION-EVALUATION-RAID-CHAIN-001` (`FACTION_EVALUATION`'s RAID selection drives `FACTION_RAID_EVALUATION` → `FACTION_RAID_DISPATCH` → resolution as one TURN-rooted lineage inside a single action — `tests/faction-evaluation-raid-chain.test.js` 8/8, 4/4 negative controls), `RESP-FACTION-RAID-LOOP-001` (orphaned `evaluateRaid`/`raidUtility`/`escalationLevel` production-wired) and the Habituation re-open (two `SOURCE_ABSENT` rows remain — the FearCore and Brain rows closed with their own extracted sources; `RESP-SOURCE-ABSENT-RECONCILIATION-001` executed with monorepo option 1 — default branch `main`, master untouched). Also open: the external Knowledge DB row, P2 world-expansion items, account credits for the autopilot's live model calls (402 — reconfirmed 2026-09-23), and a supervisor-issued stop certificate (**none exists yet** — no executor may create `docs/FEAR_AI_GLOBAL_STOP_CERTIFICATE.json`, so global completion remains unclaimed).