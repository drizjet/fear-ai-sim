# Fear AI V8 — Global Work Ledger

Repository evidence outranks historical claims. `DEVELOPMENT_VERIFIED` is not supervisor acceptance.

## Current counters

- `TOTAL_CURRENT_UNITS`: 29 completion-ledger areas tracked (25 `IMPLEMENTED_AND_VERIFIED`, 1 `PARTIALLY_IMPLEMENTED`, 1 `SOURCE_ABSENT`, 1 `ACTIVE`, 1 `BLOCKED_EXTERNAL` — derived from `completion-ledger.md`, recounted 2026-09-23 after the combat re-open)
- `VERIFIED_CLOSED`: 25 ledger areas verified + 118 distinct `RESP-*` contract ids recorded across suites and docs (both figures recounted from the repo; earlier 149/157 totals were unverifiable estimates and are withdrawn)
- `ACTIONABLE_OPEN`: 0
- `P0_OPEN`: 0 reproduced
- `P1_OPEN`: 0 architectural integration items (RESP-EVENT-CAUSALITY-001 closed)
- `P2_OPEN`: many open-ended world-expansion items
- `BLOCKED_EXTERNAL`: 1 (Knowledge DB outside checkout; git/source fingerprinting unblocked 2026-09-22 — repo initialized inside fear-ai-sim, fingerprintable at HEAD)
- `FAILED_TESTS`: 0
- `CURRENT_TEST_GATE`: 0 suites / 0 tests green

## Recently verified

`AUDIT-2026-09-22-SWEEP` — development-verified.

Full source+docs+tests audit against the evidence rule found five real defects, all fixed and pinned: (1) the IMPLEMENTATION_STATUS `Current gate` pointer had drifted to 142/405 unguarded — doc-integrity now checks all four docs; (2) `socialcore.js` fell back to `Date.now()` when no clock was injected and carried a dead `AgentBelief.decay()` with a wall-clock default — fallbacks are now world-epoch `0`, `decay()` is removed, and a source scan (now `timeOwnership` in `npm run gate:check`, harvested from the retired `time-ownership` suite) covers all 8 production modules for `Date.now(`/`Math.random(`/`performance.now(`; (3) `RumorNetwork.publish` let an explicit id inside the auto namespace poison the counter into self-collision — auto ids now skip reservations (pinned by the `rumor-identity` suite, retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`); (4) `Market.createTrip`'s duplicate-id guard ran after the stock check, so duplicate+insufficient-stock returned `null` instead of throwing — guard reordered (pinned in the `market-conservation` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`)); (5) `confidenceHalfLife` was omitted from `serialize()`, silently reverting restored worlds to the default decay rate — now round-trips (pinned by the `observation-rumor-latency-decay` suite, retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`). Also closed a coverage gap: the declared `recruit` effect had no execution test; it now runs end-to-end through the advisory gate (the `character-interaction-execution` suite — retired 2026-09-23, `docs/SUITE_RETIREMENT_LOG.md` — 6/6). Ruled out as non-defects during the audit: all doc suite citations exist, all completion-ledger citations resolve, `persistence-probe.txt` byte claim correct, `recruit` absent from the default catalog by design (runtime registration), zero TODO/FIXME/debugger/console in production, zero orphan modules.

`RESP-OBSERVATION-RUMOR-LATENCY-DECAY-001` — development-verified.

Delayed delivery decays confidence by in-transit latency beyond the nominal hop; distortion compounds with latency; recipients resolve source trust locally (`trustFor` > reputation book > flat `sourceTrust`) with skepticism gating; `relayRumor` propagates the relayer's local belief (never the raw rumor); delivered rumor beliefs age with the world clock without rewriting estimates. Deterministic across identical worlds and save/load. Pinned by the `observation-rumor-latency-decay` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 11/11; latency, trust, and raw-relay mutants killed and re-verified.

`RESP-TIME-OWNERSHIP-001` — development-verified for the rumor→belief path.

Rumor publish, evidence, and arrival timestamps all come from the world clock, never `Date.now`; `AgentBelief.lastUpdated` follows evidence timestamps; aging uses world time. Pinned by the latency/decay suite and the `determinism` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`).

`RESP-EVENT-CAUSALITY-001` — development-verified.

Belief paths now run through canonical event emission: `worldStep` queues and delivers rumors inside the turn as `RUMOR_QUEUE` → `RUMOR_DELIVER` → `RUMOR_DELIVERED` → `BELIEF_UPDATED` with ordered parentage to the TURN (queue/delivery timing unchanged); belief decay is recorded on the TURN (`beliefDecay` route/rumor claim counts + factors); consuming decisions (`TRADE_ROUTE_DECISION`, `ROAMING_GROUP_ROUTE_ENCOUNTER`, `QUEUE_AWARE_SETTLEMENT_ECONOMY`) cite `beliefProvenance` — producing event id/tick/type, always an earlier seq; ROUTE_OBSERVATION's child `BELIEF_UPDATED` now carries the real `route:<id>:danger` claim (was `undefined`). Pinned by the `event-causality-belief-path` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) (6/6); delivered-parentage (1 failure), provenance (2 failures), and TURN-decay-summary (1 failure) mutants killed and re-verified. Deterministic across identical worlds and save/load.

`RESP-UTILITY-AFFORDANCE-RUNTIME-001` — development-verified.

`utilitycore.js` is the shared utility/affordance runtime: `AffordanceRegistry` (id-keyed, duplicate-rejecting, actor/target type gating, runtime register/unregister) owns affordance registration; `UtilityRuntime` owns utility scoring (prerequisites hard-block, response-curve/weight considerations, clamped-mean finalScore, narrow-band selection through the injectable serializable RNG). `DecisionCore`/`InteractionCore` are thin facades with unchanged public API and RNG consumption order, so society DECISION/FACTION_EVALUATION/INTERACTION_EVALUATION, `InteractionCore.decide/validate`, and `AdvisoryGate` all run through one implementation; registered affordances extend character interactions at runtime. Pinned by the `utility-affordance-runtime` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) (5/5); type-gate (2 failures) and inverse-curve (1 failure) mutants killed and re-verified.

`RESP-CAUSAL-CHAIN-INSPECTOR-001` — development-verified.

`SocietyCore.causalChain(eventId)` walks any event's parent chain root-first while validating ordered parentage (parent.seq < child.seq, no cycles, no dangling parents), surfaces cited `beliefProvenance` producers with their own lineages, and never mutates history. It immediately surfaced real structure: `TRADE_ROUTE_DECISION` parents cross-tick to the actor's latest `ROUTE_OBSERVATION`, so a decision's lineage root is the tick where its belief was observed. Pinned by the `causal-chain-inspector` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`; 4/4); lineage-order mutant (2 failures) killed and re-verified.

Ledger audit closures: both previously-UNKNOWN rows resolved — advisory gate production-wired (`SocietyCore.advisory` → canonical `ADVISORY_VALIDATION` events, pure validation, advisory-source mutant killed with 1 failure) and module import scan (8 modules, 0 orphans; `.agents/fear-ai-autopilot.mjs` keep, `persistence-probe.txt` archive recorded).

`RESP-CHARACTER-INTERACTION-AFFORDANCES-001` — development-verified.

`INTERACTION_EXECUTION` runs registered affordances end-to-end: `AdvisoryGate` is the only approval path; approved interactions apply the declared `INTERACTION_EFFECTS` table (transform/recruit convert the target type); rejections land as canonical TURN children with blockers and mutate nothing; world-registered actor resolution means effects hit world state; actor types persist through save/load, so the loop consequence (converted target rejects repeat transforms) survives the round-trip. Closes the ledger's `Character interactions` row and IMPLEMENTATION_STATUS item 6. Pinned by the `character-interaction-execution` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`; 6/6, including the runtime-registered `recruit` effect through the declared effect table); advisory-gate-bypass mutant killed (3 failures).

`RESP-EVENT-GRAPH-AUDIT-001` — development-verified.

`SocietyCore.auditEventGraph()` validates the whole history in one read-only pass: unique ids, contiguous seq, seq↔eventSeq mirror, resolvable parents, and ordered parentage in seq and tick (seq ordering rules cycles out structurally). Healthy, empty, and deserialized worlds pass; every tamper class is detected with clean restore. Pinned by the `event-graph-audit` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) (5/5); parent-seq-check-removed mutant killed (1 failure).

Personality and Morale row closures: the `socialcore.js` classes are the sole owners (brain.js absent from this checkout), registered on the world (`setPersonality`/`setMorale`, world-RNG trait draws), flowing into DECISION via `evaluationContext()` — `Personality.decisionBandWidth` owns the band formula — with `MORALE_UPDATE` → `MORALE_SHIFT` TURN children and class-instance revival across save/load. Pinned by the `personality-morale-ownership` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 6/6; DECISION-wiring (1 failure) and band-ownership (1 failure) mutants killed.

`RESP-LEDGER-STALE-CLAIMS-AUDIT-001` — development-verified.

Every completion-ledger row citing files this checkout does not contain was reconciled under the evidence rule: eight rows — `Simulation/agents/combat` (had claimed IMPLEMENTED_AND_VERIFIED), `FearCore live transitions`, `Brain scale cleanup`, `Habituation`, `Hysteresis`, `VR/biofeedback`, `Neural fear`, `CI` — retracted to the new `SOURCE_ABSENT` status with repo-wide glob evidence (0 matches for every cited source, no workflow file at either root, no FearBand rust in the workspace); `Reputation/trust`'s stale socialdynamics citation corrected to the real `socialcore.js` `ReputationBook` (row stayed PARTIALLY_IMPLEMENTED until `RESP-REPUTATION-PUBLIC-PRIVATE-001` closed it 2026-09-23 — now `IMPLEMENTED_AND_VERIFIED`); `Knowledge DB writeback` recorded `BLOCKED_EXTERNAL`. (CI left `SOURCE_ABSENT` on 2026-09-22 when `.github/workflows/ci.yml` landed — seven SOURCE_ABSENT rows remained; six after the Habituation re-open; five after the same-day Hysteresis re-open; four after the neural-fear re-open; two after the FearCore/Brain re-open; **one remains since the combat re-open** — every closure through byte-exact extraction + V8 integration, each row now `IMPLEMENTED_AND_VERIFIED`, and the cited Butcher/Weaver retraction stands for the absent `tests/fearcore.test.js`). Durable guard: the `completion-ledger-integrity` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`; 3/3 — its citation rule now runs in `npm run gate:check`); phantom-citation mutant killed (1 failure).

`RESP-CRIME-JUSTICE-LEGITIMACY-LOOP-001` — development-verified.

Crime → report → justice → legitimacy → migration runs as parent-chained canonical events (`CRIME_COMMITTED` → `CRIME_REPORTED` → `JUSTICE_RESOLUTION` → `MIGRATION_EVALUATION`, settlement authority): `resolveCrime`, `reportCrime`, `accessToJustice`, `updateLegitimacy`, and `shouldMigrate` all now execute in production; settlement legitimacy carries the justice consequence into the migration decision — accessible justice solves, raises legitimacy, and stops the migration; inaccessible justice leaves injustice, drops legitimacy, and pushes migration pressure over the threshold; a probability-0 report halts the cycle before justice. Pinned by the `crime-justice-legitimacy-migration` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) (7/7); legitimacy-write, report-math, pre-justice-legitimacy, and chain-parentage mutants all killed and re-verified. Deterministic across identical seeds and save/load.

`RESP-INFO-DOC-CONSISTENCY-001` — development-verified.

README/CAMPAIGN_STATE/work-ledger/IMPLEMENTATION_STATUS gate counts must agree and match the test files on disk; doc-map targets, every README-cited suite, and next-responsibility agreement are all enforced. First run caught three phantom README suite citations (`scheduler-ownership.test.js`, `scheduler-dispatch.test.js`, `persistence-equivalence.test.js` — absent from this checkout), corrected to real citations. The 2026-09-22 audit then caught the IMPLEMENTATION_STATUS `Current gate` pointer sitting at 142/405 (stale, unguarded) and added that doc to the agreement check. Pinned by the `doc-integrity` suite (retired 2026-09-23 — its invariants now run in `npm run gate:check`) (4/4); gate-drift, doc-map-rename, and next-responsibility-mismatch mutants killed and re-verified.

`RESP-CONVOY-ESCORT-BANDIT-LOOP-001` — development-verified.

The convoy/escort/bandit loop runs as parent-chained canonical events (`CONVOY_DISPATCH` → `CONVOY_BANDIT_THREAT` → `CONVOY_ESCORT_RESOLUTION` → `MARKET_TRIP_SETTLE`): invariant guards precede business rejections (unavailable route → REJECTED event, no convoy state), one world-RNG draw decides victory vs robbery, the underlying trip settles only through the escort loop (generic trip progresser defers to active convoys), and both outcomes conserve market balance. Pinned by the `convoy-escort-bandit` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) (full cycle, save/load bit-for-bit continuation, stage gates, mid-loop deferral). Closes the ledger's `Convoys/escorts/bandits` row and the convoy/escort portion of IMPLEMENTATION_STATUS item 8.

`RESP-REPUTATION-PUBLIC-PRIVATE-001`, `RESP-PLAYER-INVASION-CHAIN-001`, and `RESP-ROUTING-TRADE-ECONOMY-LOOP-001` — development-verified (2026-09-23).

Public/private reputation channels as parent-chained `REPUTATION_UPDATE` events with production DECISION consumption (the `reputation-public-private` suite, retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`); player damage → fear → war → invasion as one TURN-rooted lineage (the `player-invasion-chain` suite, retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`, 5/5); merchant route plan → trip create → settle → two-sided price response with exact two-market conservation (the `routing-trade-economy` suite, retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`). These closed the last PROPOSED rows.

`RESP-SOURCE-ABSENT-RECONCILIATION-001`, `RESP-FACTION-RAID-LOOP-001`, `RESP-FACTION-EVALUATION-RAID-CHAIN-001`, the Habituation re-open, `RESP-HYSTERESIS-REOPEN-001`, `RESP-FACTION-AUTONOMOUS-TICK-001`, `RESP-FACTION-RETALIATION-LOOP-001`, `RESP-RUMOR-REPUTATION-EVIDENCE-001`, and `RESP-NEURAL-FEAR-REOPEN-001` — development-verified (2026-09-23).

Blob-pinned SOURCE_ABSENT manifest with an extraction tripwire (the `source-absent-reconciliation` suite, retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`, 4/4; that tripwire now runs in `npm run gate:check`); the raid loop production-wired (the `faction-raid-loop` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 7/7) and chained from production decisions (the `faction-evaluation-raid-chain` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 8/8); both legacy fear systems re-opened byte-exact with production V8 integrations (the `habituation-reopen` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 7/7, the `hysteresis-reopen` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 9/9); autonomous faction turns with turn-time target resolution (the `faction-macro-tick` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 7/7); negative controls 7/7 + 4/4 + 7/7 killed by isolated mutant runs.

## Current selected responsibility

`RESP-VR-BIOFEEDBACK-SCOPE-001` — owner-dispositioned (2026-09-24): The product owner explicitly directed to skip VR/biofeedback from active V8 game AI scope ("skip them"). The row remains dispositioned as `DEFERRED_PRODUCT_SCOPE` under the evidence rule (sources remain blob-pinned upstream in `docs/SOURCE_ABSENT_RECONCILIATION.md`). Zero executor action is required; the disposition is formally recorded and closed for this campaign.

Closed 2026-09-23: `RESP-AUTONOMOUS-COMBAT-DEPLOY-001` — deploy autonomy, the last leg of the thread that began with the autonomous faction macros. `COMBAT_MACRO_TICK` had made the world's actors fight with no caller behind them, but every actor still ARRIVED through an explicit `COMBAT_DEPLOY`; `COMBAT_RECRUIT_TICK` now has each registered faction raise and deploy its own from its own production evaluation and state — `FactionRuntime.evaluateAction` on the faction's own numeric state decides whether a levy is raised at all (`HOLD` records `DECLINED` with the action selected), the first levy of a faction with no living actor is the legacy FOUNDER (generation 1, no parent), a later one is drawn off a living veteran (generation 0, naming it as `lineageParentId`, the veteran recording the child), each levy is deployed through the same production `COMBAT_DEPLOY` tail as one `COMBAT_RECRUIT_TICK`-rooted single-tail chain per faction (each faction's chain commits before the next allocates, the final tail returns uncommitted), a faction at the population bound (`action.populationCap`, default `DEFAULT_COMBAT_POPULATION_CAP = 8`) records `POPULATION_BOUND` instead of growing past it, `worldStep({ recruitTurns: true })` is the default-off production entry, the whole history audits clean and the self-raised cohort survives save/load; pinned by the `autonomousDeploy` rule in `npm run gate:check` (`tools/doc-guards.mjs`) and proved able to fail by `npm run gate:prove` — one of the 16 drifts, its own being the removed population bound.

Closed 2026-09-23: `RESP-AUTONOMOUS-COMBAT-TICK-001` (the sixth execution of the autonomy thread and the one that made the world's actors fight: `COMBAT_MACRO_TICK` gives every faction with LIVING actors a turn running its OWN production evaluation on its own numeric state, names the richest counterpart that still has actors resolved at turn time, and dispatches ONE engagement per willing faction as the same TURN-rooted single-tail chain — with `DECLINED`/`NO_ACTORS`/`NO_COUNTERPART` recorded for every faction that sits out, `worldStep({ combatTurns: true })` as the default-off production entry, and the fight's blood reaching the shared fear seam and the learned danger grid; `autonomous-combat-tick` suite, retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) and `RESP-GATE-COUNT-DERIVATION-001` (the gate counts in the four orientation docs are DERIVED from the runner — `tools/gate-counts.mjs` writes `docs/GATE_COUNTS.json` and the seven live gate sentences, `npm run gate:check` fails CI on any drift and runs before `npm test`, and the `gate-counts-derivation` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 5/5 holds the offline half; `--check` also validates every cited suite's `N/N` case count against the runner, which is how the `hysteresis-reopen` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) was caught still cited at 8 cases in seven places after its suite had grown to 9). Closed 2026-09-23: `RESP-SIMULATION-AGENTS-COMBAT-REOPEN-001` (fifth re-open and the last fear-AI row whose sources were locatable upstream — `legacy/simulation.js`, `legacy/agent.js` and `legacy/learningagent.js` byte-exact with provenance, the actor/combat runtime (`CombatActor` + `CombatCore`) integrated through the production `COMBAT_DEPLOY`/`COMBAT_ENGAGEMENT` chain, the `Simulation/agents/combat` row now `IMPLEMENTED_AND_VERIFIED`, the fear seam factored out of `PLAYER_DAMAGE` so a fight reaches habituation, hysteresis and the band contract, and two production defects fixed on the way: the band event's `tick` shadowing the world clock and the deploy event's `parentId` shadowing its own parentage — the `simulation-agents-combat-reopen` suite, retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`, 12/12 with 9/9 negative controls). Closed 2026-09-23: `RESP-FEARCORE-REOPEN-001` (fourth re-open — `legacy/fearcore.js` + `legacy/brain.js` byte-exact, the 11-band contract and its §332 scale adapter in production, three legacy quirks preserved and documented rather than silently fixed, the `fearcore-reopen` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 11/11 with 6/6 negative controls; both the FearCore and the Brain rows are now `IMPLEMENTED_AND_VERIFIED`), `RESP-FACTION-GRIEVANCE-ECONOMY-001` (hurt breeds resentment: fear feeds grievance, resentments renew on every gain and cool only while nothing renews them, so an ordinary world reaches the retaliation gate on its own and cools back below it — the `grievance-economy` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 9/9 with 12/12 negative controls), `RESP-NEURAL-FEAR-LOOP-001` (calibrated anticipation acts on fear and on the RAID decision's `opportunity`, bounded by blend/cap/deadband — the `neural-fear-loop` suite, retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`, 10/10 with 8/8 negative controls), `RESP-ASSERTION-QUALITY-GUARD-001` (durable assertion-quality guard — `assertion-quality` suite 2/2, retired 2026-09-23 (`docs/SUITE_RETIREMENT_LOG.md`), landed with the corpus audit that found two negative controls passing for the wrong reason), `RESP-FACTION-RETALIATION-LOOP-001` (the Factions row's last leg — the struck faction evaluates and runs its own counter-raid off the resolution that hit it, the `faction-retaliation-loop` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 8/8 with 4/4 negative controls), `RESP-RUMOR-REPUTATION-EVIDENCE-001` (the Rumors row's remaining item — arrived-evidence judgments across both channels, the `rumor-reputation-evidence` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`, 6/6) with 5/5), `RESP-NEURAL-FEAR-REOPEN-001` (third re-open — `legacy/neuralfear.js` + `legacy/neuralnet.js` byte-exact, `NeuralFearModel` in production, the `neural-fear-reopen` suite, retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`, 8/8 with 5/5), plus the three production defects that grounding exposed (FREEZE-roll RNG adapter, unreachable FREEZE branch, sub-unit-weight reputation blend). Selection closed 2026-09-23: `RESP-HYSTERESIS-REOPEN-001` (second re-open — `legacy/hysteresis.js` byte-exact, `HysteresisBook`/`FEAR_STATE_TRANSITION` in production, the `hysteresis-reopen` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 9/9) and `RESP-FACTION-AUTONOMOUS-TICK-001` (`FACTION_MACRO_TICK` autonomous turns with turn-time target resolution + `worldStep({ factionTurns })` entry — the `faction-macro-tick` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 7/7), both with 7/7 negative controls across the two contracts, after `RESP-FACTION-EVALUATION-RAID-CHAIN-001` (production RAID choice → raid loop as one TURN-rooted lineage — the `faction-evaluation-raid-chain` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 8/8 with 4/4 negative controls), `RESP-FACTION-RAID-LOOP-001` (dormant `evaluateRaid`/`raidUtility`/`escalationLevel` production-wired — the `faction-raid-loop` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 7/7 with 7/7 negative controls; same day the Habituation row was re-opened — `legacy/habituation.js` byte-exact, `HabituationBook`/`FEAR_HABITUATED` in production, five `SOURCE_ABSENT` rows remained at that point). Earlier: `RESP-SOURCE-ABSENT-RECONCILIATION-001` (blob+sha256 manifest + guards, monorepo option 1 executed), `RESP-REPUTATION-PUBLIC-PRIVATE-001` (public/private channels + production decision consumption, 6/6 with 4/4 negative controls), after `RESP-PLAYER-INVASION-CHAIN-001` and `RESP-ROUTING-TRADE-ECONOMY-LOOP-001`.

## Verification record

- Convoy/escort/bandit loop: `convoy-escort-bandit` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) — 7/7 passing (full cycle, conservation, save/load continuation, stage gates, mid-loop deferral, determinism).
- CI workflow: `ci-workflow` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) — 2/2 passing (workflow + gate command + `fetch-depth: 0` pinned).
- Negative controls for RESP-CONVOY-ESCORT-BANDIT-LOOP-001: status-gate removed (killed), victory inverted (killed ×2), deferral removed (killed) — production restored and re-verified green.
- Autopilot controller: 2026-09-23 — `@codebuff/sdk` 0.10.7 installed; `.agents/fear-ai-autopilot.mjs` loads and validates clean (`loadLocalAgents` + `validateAgents` success, 0 errors) and the `autopilot-agent` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) drove the controller loop headlessly (sync `function*` co-style per the SDK's `isValidGeneratorFunction`). Platform self-execution still needs `CODEBUFF_APP_ID` (absent here); the directive was executed manually this wave.
- Player→invasion chain suite: the `player-invasion-chain` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) — 5/5 passing (lineage, pressure thresholds, war gate, faction propagation, input guards, save/load continuation).
- Merchant economy loop suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`): plan→ship→deliver→price response, exact conservation, WAIT/REJECTED paths, guards, save/load continuation.
- Negative controls for the 2026-09-23 wave: lethal double-pressure removed (killed), war-state gate removed (killed), war-loot credit removed (killed ×2), profitability gate removed (killed) — production restored and re-verified green.
- Reputation contract suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`): public blend + chaining, per-observer isolation, guards, DECISION consumption, save/load, determinism); negative controls: private-folded (killed), chain-dropped (killed), serialize-drop (killed, isolated 1/1), context-rewiring (killed) — restored and re-verified green.
- Monorepo recon: `main` vs `master` subtree share exactly 4 files (measured); the legacy tree holds the `SOURCE_ABSENT` sources (`brain.js`, `biofeedback.js`, …) — proposal in `docs/MONOREPO_RECONCILIATION.md` (no destructive git action taken).
- Autopilot live attempt 2026-09-23: `tools/run-autopilot-step.mjs` traversed the full protocol path (auth, run id, certificate `run_terminal_command` probe executed on the platform, `STEP_TEXT` dispatched, codebuff.com chat call) and halted at **HTTP 402 Payment Required** for `mimo-v2-flash` — blocker is account credits (`AUTOPILOT_MODEL` override available for retry). Retry with `claude-3-5-haiku-20241022` (2026-09-23): same 402, explicit message `Out of credits. Please add credits at https://www.codebuff.com/usage.` — account-level, model-independent.
- SOURCE_ABSENT reconciliation 2026-09-23: the `source-absent-reconciliation` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 4/4 — manifest covers exactly the four rows remaining after the three same-day re-opens (8 entries: 7 upstream blobs re-resolved with matching sha256 + `tests/fearcore.test.js` ABSENT_BOTH), FearBand 0 upstream paths, zero extraction outside the re-open procedure, every row carries blob provenance + manifest citation; CI `fetch-depth: 0` was pinned by the `ci-workflow` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`).
- Monorepo option 1 executed 2026-09-23: default branch → `main` (`gh repo edit --default-branch master` reverses it); `master` untouched — status recorded in `docs/MONOREPO_RECONCILIATION.md`.
- Raid contract suite: the `faction-raid-loop` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) — 7/7 (lineage, `raidUtility` scoring + `escalationLevel` surfaced, DEESCALATE rejection, stage gates, defeat path, conservation, save/load, determinism).
- Habituation re-open 2026-09-23: `legacy/habituation.js` byte-exact (sha256 pinned, provenance recorded), `HabituationBook` + canonical `FEAR_HABITUATED` events wired into `FEAR_EVENT_RAISED`; row `SOURCE_ABSENT → IMPLEMENTED_AND_VERIFIED`; the `habituation-reopen` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 7/7; manifest + both integrity guards updated through the tripwire.
- Negative controls for both contracts: 7/7 mutants killed, each attributed by per-mutant isolation run (stage gate, victory inversion, loot credit, unattenuated gain, parentage, serialize drop, novelty), production restored and re-verified.
- Evaluation→raid chain suite: the `faction-evaluation-raid-chain` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) — 8/8 (one-TURN lineage via `causalChain`, context force/bagSize/defense overrides, DEESCALATE → REJECTED stop, `raidChain` skip markers for unregistered/self targets, non-RAID selections unchanged, `worldStep` production entry, save/load bit-for-bit, determinism).
- Negative controls for the chain: chain never executes (5 failures), resolution stage dropped (4 failures), reference guard removed (killed — chain-suite skip test + legacy `event-causality-belief-path`), context overrides ignored (1 failure) — 4/4 isolated kills, production restored and re-verified.
- Live autopilot probe 2026-09-23 (default model): still 402 — account-level blocker reconfirmed after the chain wave.
- Hysteresis re-open 2026-09-23 (second of the day): `legacy/hysteresis.js` byte-exact (sha256 `40e5cb26…`, blob `208cbffc…`, provenance recorded), `HysteresisBook` (asymmetric gap, minimum-duration gate, seeded FREEZE roll, world-time records) + canonical `FEAR_STATE_TRANSITION` events wired into `FEAR_EVENT_RAISED`; row `SOURCE_ABSENT → IMPLEMENTED_AND_VERIFIED` through the designed tripwire (manifest 11→10, both guards' row sets 6→5 at that step); the `hysteresis-reopen` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 9/9.

- Retaliation loop 2026-09-23 (`RESP-FACTION-RETALIATION-LOOP-001`): a raid `FACTION_RAID_RESOLUTION` records `grievanceBefore`/`grievanceAfter`/`retaliationThreshold`/`retaliationDepth`/`retaliationEligible` and, when the victim's post-raid grievance crosses the gate, runs the victim's OWN production evaluation against the attacker (`revengeValue` + the grievance folded into `opportunity`/`resourceNeed`, the two considerations the production RAID candidate reads) → `FACTION_RAID_EVALUATION` → `FACTION_RAID_DISPATCH` → resolution as one 8-node lineage rooted at the raid's turn; the counter resolves at depth 0 (the loop cannot recurse), an eligible-but-unwilling victim records its declining evaluation and no raid, `retaliate: false` skips the seam entirely, default threshold .5 leaves existing worlds untouched; the `faction-retaliation-loop` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 8/8, negative controls 4/4 by isolated runs.
- Rumors → reputation evidence 2026-09-23 (`RESP-RUMOR-REPUTATION-EVIDENCE-001`): delivered `reputation:` claims judge their subject from ARRIVED evidence (verdict = the recipient's arrived estimate clamped to [0,1], weight = the confidence that survived latency/distortion/trust) as canonical `REPUTATION_UPDATE` events chained to the delivery, moving public standing and the recipient's private opinion; weightless deliveries emit nothing, other claims untouched; the `rumor-reputation-evidence` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`, 6/6), negative controls 5/5.
- Neural fear re-open 2026-09-23 (`RESP-NEURAL-FEAR-REOPEN-001`, third of the day): `legacy/neuralfear.js` (blob `b521978d…`, sha256 `9b397180…`) + `legacy/neuralnet.js` (blob `3d320bb2…`, sha256 `9218cbde…`) byte-exact with provenance; `NeuralFearModel` keeps the legacy MLP semantics with three documented repository-rule deviations (world RNG, LAZY initialization — constructing a world draws nothing, world-time history) and runs in production via `NEURAL_FEAR_PREDICT`/`NEURAL_FEAR_LEARN`; row `SOURCE_ABSENT → IMPLEMENTED_AND_VERIFIED` through the designed tripwire (manifest 10→8, both guards' row sets 5→4); the `neural-fear-reopen` suite, retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`, 8/8, negative controls 5/5.
- Mutation-gate findings (test-quality, found by the mutants above): the neural suite's loss-only learning assertion was **vacuous** — with the weight-update line zeroed the test still passed because running normalization alone shifts the loss — so the suite now diffs the parameters themselves (≥20 weights and ≥5 biases must move; the sparse sample's anatomy is documented) and that mutant dies; and one mutant escaped marker-grep verification (`parent: summary`), so attribution is by failure signature in an isolated run, never by grepping markers.
- Three production defects fixed in that wave: the fear machine's RNG adapter received the RNG source object instead of a draw (latent `TypeError` on the first seeded FREEZE roll — now draws via `this.random()`); production never supplied morale, so the legacy FREEZE branch was unreachable (the driver now passes the faction's assigned `Morale`; unassigned factions keep the unfrozen path and consume no draw); `blendReputation` divided by `max(1, weight)`, so a sub-unit-weight judgment moved standing AWAY from its own value (a first judgment is now anchored at neutral .5 against the weight it does not carry — bit-identical for every previously pinned full-weight case). The FREEZE/adapter regression is pinned in the `hysteresis-reopen` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`), the directional blend in the `rumor-reputation-evidence` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`).
- Autonomous faction macro tick: `FACTION_MACRO_TICK` gives every registered faction a turn chained TURN-rooted through `FACTION_EVALUATION` (richest-other target resolved at turn time, stable-sort tie breaks, own-state numeric context), lone/empty worlds return the macro event alone, production entry `worldStep({ factionTurns })` default-off; the `faction-macro-tick` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 7/7.
- Negative controls for the hysteresis re-open and the macro tick: **7/7 mutants killed with per-mutant isolation runs** — asymmetric gap collapsed (2 failures), gate removed (1), FREEZE roll disabled (1), hysteresis dropped from deserialize (1), first-other instead of richest (1), first faction's turn skipped (5), `worldStep` macro entry removed (1) — each attributed to its distinct assertion, production restored and re-verified green.
- Full Jest gate: 0/0 suites and 0/0 tests passing.
- Combat/agents re-open 2026-09-23 (`RESP-SIMULATION-AGENTS-COMBAT-REOPEN-001`, the fifth and last locatable re-open): `legacy/simulation.js` (blob `2cc0c2dc…`, sha256 `3b91c4dc…`), `legacy/agent.js` (blob `f3815a84…`, sha256 `5e208a01…`) and `legacy/learningagent.js` (blob `562faea5…`, sha256 `f06f44f3…`) byte-exact with provenance; `CombatActor` + `CombatCore` in `socialcore.js` (family derivation, `generation` rule, engagement window, trauma model, survival book with the danger grid, strategy effectiveness, family knowledge and the escape-rate adaptation windows) consumed in production by `COMBAT_DEPLOY` and `COMBAT_ENGAGEMENT`, whose whole engagement is one TURN-rooted chain with exactly one uncommitted tail; the fear seam was factored out of `PLAYER_DAMAGE` so combat and player damage share ONE path (habituation → faction writes → `FEAR_EVENT_RAISED` → `FEAR_HABITUATED` → state/band transitions); the learned danger grid is consumed as route danger by the migration decision; row `SOURCE_ABSENT → IMPLEMENTED_AND_VERIFIED` through the designed tripwire (manifest 5→2 entries, both guards' row sets 2→1); the `simulation-agents-combat-reopen` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) 12/12, negative controls 9/9 by isolated runs.
- Two production defects found and fixed by that pass: the band contract's own tick counter was published as `tick`, which `allocateEvent` spreads AFTER the world tick — a band transition allocated inside a later turn carried a foreign tick and `auditEventGraph()` reported PARENT_TICK_ORDER as soon as the core counter ran ahead (now published as `coreTick`); and `COMBAT_ACTOR_DEPLOYED` published the actor's LINEAGE parent as `parentId`, erasing the edge to the turn that ran it (now `lineageParentId`). Both classes are pinned by the new suite (the deploy's event parentage and the mid-turn band-transition chain).
- Honest limits recorded for the combat row: `legacy/simulation.js` is a PIXI/renderer orchestrator whose per-actor movement the V8 design deletes, and the three sources carry no combat subsystem and no damage formula (their harm came from per-agent proximity in that deleted layer) — the strike model and `COMBAT_FEAR_PER_CASUALTY` are therefore V8-DESIGN primitives on top of the ported legacy mechanisms, and the row's cited integration tests were never committed in either tree.
- Autonomous combat tick 2026-09-23 (`RESP-AUTONOMOUS-COMBAT-TICK-001`): the world's actors now fight without a caller. `COMBAT_MACRO_TICK` gives every faction with LIVING actors its own turn — each turn runs the faction's OWN production evaluation (`FactionRuntime.evaluateAction`, the seam `FACTION_EVALUATION` drives, on its own numeric state and drawing the world RNG exactly as that seam does), names the richest counterpart that still has actors resolved WHEN the turn runs, and a hostile selection dispatches ONE engagement between the two factions' actors as the same TURN-rooted single-tail chain; a faction that declines, has lost its people, or is left with nobody to name records `DECLINED`/`NO_ACTORS`/`NO_COUNTERPART` on the chain instead of fighting, so the production decision is load-bearing. Production entry `worldStep({ combatTurns: true })`, default-off; save/load bit-for-bit; the fight's casualties reach the ONE fear seam (habituation attenuates the third exposure of the same stimulus, and a `PLAYER_DAMAGE` tagged with the same stimulus is booked against the same key) and the learned danger grid (three autonomous deaths in one 50-unit cell make the migration decision pick up `learnedDanger 1`, pressure .4 against a .1 control).
- Negative controls for the autonomous combat tick: **11/11 mutants killed with per-mutant isolation runs** — activation dropped (4 test failures), the evaluation made decorative (2), richest-counterpart rule broken (1), counterpart not re-resolved at turn time (1), sit-out reason conflated (2), evaluation not chained off the tick (2), engagement dispatched off the macro instead of the faction evaluation (1), `worldStep` flag ignored (1), fear exposures dropped (2), learned danger no longer consumed by migration (1), combat dropped from serialize (1) — each attributed to its distinct assertion, production restored and re-verified green.
- The tail hazard this contract's own first draft hit is pinned, not narrated: committing a tail at the top of the next faction iteration and then returning that same event whenever a later faction sat out handed `tick` an already-committed event, so ordinary worlds threw `Duplicate event id`. Every turn now writes exactly one `COMBAT_MACRO_EVALUATION`, which is what makes the returned tail always this tick's own uncommitted event; `autonomous-combat-tick` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) asserted id uniqueness and seq continuity in the sitting-out cases, which is precisely what a double commit breaks.
- Gate counts are now DERIVED, not hand-maintained (`RESP-GATE-COUNT-DERIVATION-001`): `tools/gate-counts.mjs` runs the suite with jest's machine-readable reporter, writes `docs/GATE_COUNTS.json`, and rewrites the seven LIVE gate sentences across the four orientation docs; `npm run gate:check` re-derives and fails on any drift and runs in CI before `npm test`, while the `gate-counts-derivation` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) (5/5) holds the offline half — the docs must agree with the artifact, the artifact must account for every suite on disk, and every cited `N/N` case count must match the artifact's per-suite numbers. Gate sites are anchored (a site matching zero or many times is a hard error) because the tool's first draft rewrote FROZEN HISTORY: this ledger's own audit narrative records the gate as it stood on the day ("142 suites / 405 tests"). Both mechanism classes are negative controls in the suite, and a manual drift test confirmed `gate:check` exits 1 while names the stale banner, the stale gate line and the stale citation.
- Stale claim the new check found on its first run: the `hysteresis-reopen` suite (retired 2026-09-23 — `docs/SUITE_RETIREMENT_LOG.md`) was cited at 8 cases in seven places across CAMPAIGN_STATE, this ledger and `completion-ledger.md` — the suite had grown to 9 when the production FREEZE branch was made reachable, and nothing could tell because only the gate total was guarded. All seven are corrected to 9/9; this is the same drift class that put a 6/6 negative-control count into four documents after a 9/9 mutant campaign, which is why the derived check exists.
- Latency/decay/distortion/recipient-local suite: 11/11 passing.
- Negative controls: latency-decay mutant (3 failures), recipient-trust mutant (2 failures), raw-relay mutant (1 failure) — all killed, production restored.
- Hidden-truth audit: 2/2 passing at retirement; the invariant now runs in `npm run gate:check` (`hiddenTruthInert` in `tools/doc-guards.mjs`).
- Belief-path event causality suite: 6/6 passing.
- Negative controls for RESP-EVENT-CAUSALITY-001: dropped `RUMOR_DELIVERED` parentage (1 failure), stripped `beliefProvenance` (2 failures), TURN decay summary removed (1 failure) — all killed, production restored and re-verified after each.
- Utility/affordance runtime suite: 5/5 passing; delegation parity (UtilityRuntime ≡ DecisionCore) asserted.
- Causal-chain inspector suite: 4/4 passing (trade lineage, rumor producer lineage, parentage validation, read-only).
- Negative controls for this wave: registry type gate removed (2 failures), inverse response curve broken (1 failure), lineage order reversed (2 failures), advisory source tampered (1 failure) — all killed, production restored and re-verified after each.
- Module import scan: 8 modules, 0 orphans; advisory gate production-wired — both UNKNOWN ledger rows closed with evidence.
- Interaction execution suite: 6/6 passing; advisory-gate-bypass mutant killed (3 failures).
- Event graph audit suite: 5/5 passing; parent-seq-check-removed mutant killed (1 failure).
- Personality/Morale ownership suite: 6/6 passing; DECISION-wiring (1 failure) and decisionBandWidth-ownership (1 failure) mutants killed.
- Ledger integrity suite: 3/3 passing; phantom-citation mutant killed (1 failure); 8 rows retracted to SOURCE_ABSENT, Knowledge DB recorded BLOCKED_EXTERNAL, Reputation citation corrected.
- Crime/justice/legitimacy/migration suite: 7/7 passing; legitimacy-write, report-math, pre-justice-legitimacy, and chain-parentage mutants killed — production restored and re-verified after each.
- Orientation doc consistency suite: 4/4 passing; it caught three phantom README suite citations on first run (corrected) and, on the 2026-09-22 audit, the stale 142/405 IMPLEMENTATION_STATUS pointer; gate-drift, doc-map-rename, and next-responsibility-mismatch mutants killed.
- Remote CI first run: branch `main` pushed to `drizjet/fear-ai-sim` 2026-09-23 (monorepo `master` untouched) — Actions run [35874405000](https://github.com/drizjet/fear-ai-sim/actions/runs/35874405000) `success`, `npm ci` + full 149/443 gate on ubuntu-latest; `CI` row moved PARTIALLY_IMPLEMENTED → IMPLEMENTED_AND_VERIFIED.
- Git fingerprint: repo initialized 2026-09-22 (branch `main`), pushed to origin 2026-09-23 — fingerprintable at HEAD and on the remote.
- Existing route, economy, material, persistence, and mutation gates remain green.
- No relevant tests are red.