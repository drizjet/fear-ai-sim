# Fear AI Completion Ledger

Updated 2026-09-23 from the current checkout. Status is based on source/runtime/test evidence, not historical claims.

| Area | Status | Evidence | Remaining |
|---|---|---|---|
| Simulation/agents/combat | IMPLEMENTED_AND_VERIFIED | Re-opened 2026-09-23 (fifth re-open): all three located sources extracted byte-exact — `legacy/simulation.js` (upstream blob 2cc0c2dc991362fece0afea05150bcd988fe4cb2), `legacy/agent.js` (blob f3815a84736b04afc592ee3fd8652464e898b148) and `legacy/learningagent.js` (blob 562faea5106f6cff3ccdc9cecb7b3dd7a0d2a7c6), every sha256 in `legacy/PROVENANCE.md` — and given the implement-and-test pass it always needed: `CombatActor` + `CombatCore` in `socialcore.js` are the society world's actors (legacy lineage, engagement window, trauma memory) and its survival book (escapes/deaths, strategy effectiveness, learned danger grid, family knowledge, escape-rate adaptation), consumed in production by `COMBAT_DEPLOY` and `COMBAT_ENGAGEMENT` — one engagement is a single TURN-rooted chain (start → one strike per side per round → casualties → fear exposures → resolution) ending in exactly one uncommitted tail. Ported verbatim: the family derivation, the `generation` rule, trauma `min(1, fear * 0.8)` with `0.9995` decay and the `> 0.3` floor lifted onto the actor's felt fear, the `danger > 2` gate on 50-unit cells (consumed as route danger by `MIGRATION_EVALUATION`), the six strategies with their `.5` prior, four context bonuses and the choice-counts-as-a-use rule, the `.05` survival-time EMA, the escape-rate adaptation windows scaling the winner's confidence step, and the 50/100 caps on hiding spots and family knowledge. Fear reaches production through the ONE fear seam (factored out of `PLAYER_DAMAGE` in this change), so habituation, hysteresis and the band contract all see a fight. **Limits recorded, not papered over:** `legacy/simulation.js` is a PIXI/renderer orchestrator whose per-actor movement the V8 design deletes; the three sources carry no combat subsystem and no damage formula (their harm came from per-agent proximity in that deleted layer), so the strike model and `COMBAT_FEAR_PER_CASUALTY` are V8-DESIGN primitives on top of the legacy mechanisms; the row's cited integration tests were never committed in either tree — absence recorded, not recoverable. **Two production defects found and fixed by this pass:** the band contract's own tick counter was published as `tick`, overwriting the world tick inside `allocateEvent` (a foreign tick reached the graph and `auditEventGraph()` reported PARENT_TICK_ORDER as soon as the core counter ran ahead — now `coreTick`), and the deploy event published the actor's LINEAGE parent as `parentId`, erasing the edge to the turn that ran it (now `lineageParentId`). Pinned by `tests/simulation-agents-combat-reopen.test.js` (12/12) | None identified for current scope; the disposition under the evidence rule is the ported-mechanism set above, with the strike model and the deleted spatial layer recorded as documented V8 design rather than legacy ports |
| FearCore live transitions | IMPLEMENTED_AND_VERIFIED | Re-opened 2026-09-23 (fourth re-open of the day): legacy/fearcore.js extracted byte-exact (upstream blob 185494c831a6688a70aa4d143e18757a7cceb7eb, sha256 in legacy/PROVENANCE.md) and legacy/brain.js (blob 163dfa7a184fda3e831b19ab7fa8cb65f02b3b1e); the 11-band contract (core 4 raw-fear + extended 7 context-driven) is ported to socialcore.js with thresholds verbatim (ALERT .8 / ANXIOUS 1.4 / PANIC 3.8; exits .55/.8/1.2), panicLockTicks 10 behind the PRESENCE_BREAK bypass, the AGGRESSIVE/HIDE/FREEZE/VAULTING/CRAWLING/RECOVER rules, the force-fallback for leaving an extended band, the snap-to-CALM guard and a bounded decisionTrace, plus the §332 scale adapter from the extracted brain source (fearScale: normalized 0..1 → raw 0..3.8) verbatim; production drives it at the fear seam so a real band change is a canonical FEARCORE_BAND_TRANSITION chained off the exposure that produced the level it read (hysteresis transition staged first when both machines speak), round-trips save/load and consumes no RNG unless the contract's own PANIC rules fire. Three legacy quirks preserved and documented rather than silently fixed: an unreachable CRAWLING-exit disjunct, HIDE-entered-from-PANIC escaping before CRAWLING can trigger, and the §260 stay rule holding RECOVER (its progress branch is unreachable). The row's cited legacy test was never committed in either tree — recorded as a limitation, not recoverable | None identified for current scope; Rust parity would still require a real FearBand source (0 upstream paths, ls-tree probe) |
| Brain scale cleanup | IMPLEMENTED_AND_VERIFIED | Re-opened 2026-09-23 with the FearCore row (they share the source): legacy/brain.js extracted byte-exact (upstream blob 163dfa7a184fda3e831b19ab7fa8cb65f02b3b1e, sha256 in legacy/PROVENANCE.md) and read, so the row no longer rests on an absent source; the only V8-relevant mechanism it carries is the normalized→raw fear-band scaling, ported verbatim as fearScale and pinned in tests/fearcore-reopen.test.js against the documented 0.21/0.37/1.0 threshold crossings. The "scale cleanup" claim itself stays RETRACTED: V8 has no brain module to scale (deletes-by-design), and the legacy module is evidence, never a port target | None identified for current scope (the cleanup remains retracted; no V8 brain module exists by design) |
| Habituation | IMPLEMENTED_AND_VERIFIED | Re-opened 2026-09-23 via the re-open procedure in docs/SOURCE_ABSENT_RECONCILIATION.md: legacy source extracted byte-exact to legacy/habituation.js (upstream blob df69efd8bf62c1b7ac38161fcdb9e9909e764c5d, sha256 recorded in legacy/PROVENANCE.md and verified in tests/habituation-reopen.test.js); V8 integration HabituationBook in socialcore.js (novelty-protected attenuation, world-time recovery, zero wall-clock) consumed in production by FEAR_EVENT_RAISED, every exposure recorded as a canonical FEAR_HABITUATED event parent-chained to its fear event; both states round-trip save/load | tests/habituation-reopen.test.js (7/7): byte-exact extraction, legacy semantics (novelty/cap/decay-speed/world-time recovery), production chain + causal lineage, per-faction/per-source isolation, no-faction bypass, save/load continuation, determinism |
| Hysteresis | IMPLEMENTED_AND_VERIFIED | Re-opened 2026-09-23 via the re-open procedure in docs/SOURCE_ABSENT_RECONCILIATION.md: legacy source extracted byte-exact to legacy/hysteresis.js (upstream blob 208cbffc038a04a901b262024d5ad414b9dcdcd9, sha256 recorded in legacy/PROVENANCE.md and verified in tests/hysteresis-reopen.test.js); V8 integration HysteresisBook in socialcore.js (asymmetric enter/exit thresholds, minimum-duration gate, seeded FREEZE roll, world-time records) consumed in production by FEAR_EVENT_RAISED, every real transition recorded as a canonical FEAR_STATE_TRANSITION event parent-chained to the exposure that produced the level it read; machine state round-trips save/load | tests/hysteresis-reopen.test.js (8/8): byte-exact extraction, legacy gap/gate/FREEZE semantics, production chain + causal lineage, per-faction isolation, save/load continuation, determinism |
| Utility/affordances | IMPLEMENTED_AND_VERIFIED | `utilitycore.js` (`AffordanceRegistry` + `UtilityRuntime`) owns registration, type gating, curve/weight scoring, and band selection; `decisioncore`/`interactioncore`/`advisorygate` delegate with unchanged API and RNG order; production paths (society DECISION/FACTION_EVALUATION/INTERACTION_EVALUATION, advisory gate) run through it; `tests/utility-affordance-runtime.test.js` | None identified for current scope |
| Personality | IMPLEMENTED_AND_VERIFIED | `socialcore.js` `Personality` is the sole owner (the historical brain module is absent from this checkout): OCEAN + derived traits, world-RNG draws via world registry `setPersonality`; flows into DECISION where the class owns `decisionBandWidth` (plain-object fallback preserved); revives as a class instance across save/load; `tests/personality-morale-ownership.test.js` | None identified for current scope |
| Morale | IMPLEMENTED_AND_VERIFIED | `socialcore.js` `Morale` is the sole owner (the historical brain module is absent from this checkout): world registry `setMorale` + `MORALE_UPDATE` → canonical `MORALE_SHIFT` TURN children (before/after/delta/freezeRisk); morale flows into DECISION considerations via `evaluationContext`; revives as a class instance across save/load; `tests/personality-morale-ownership.test.js` | None identified for current scope |
| Beliefs/evidence | IMPLEMENTED_AND_VERIFIED | observation→belief (`recordRouteObservation`) and rumor→belief (`spread`/`deliverDue`/`relayRumor`) production paths pinned; recipient-local trust (`trustFor`/reputation/skepticism); latency/decay/distortion suite `tests/observation-rumor-latency-decay.test.js`; belief paths through canonical event emission with decision provenance (`tests/event-causality-belief-path.test.js`) | None identified for current scope |
| Rumors | IMPLEMENTED_AND_VERIFIED | monotonic ids (auto ids skip explicitly reserved ones — an explicit `rumor-1` can no longer poison the counter), delayed delivery with latency decay, TTL expiry, distortion, relay-through-belief, save/load incl. `confidenceHalfLife`; `tests/rumor-identity.test.js`, `tests/rumor-transport.test.js`, `tests/observation-rumor-latency-decay.test.js`; delivered `reputation:` claims now judge their subject from ARRIVED evidence (verdict = the estimate the recipient ended up believing, weight = the confidence that survived latency/trust; weightless delivery emits nothing; other claims unchanged) moving BOTH public standing and the recipient's private opinion as canonical REPUTATION_UPDATE events chained to the delivery that carried them — `tests/rumor-reputation-evidence.test.js` | None identified for current scope |
| Reputation/trust | IMPLEMENTED_AND_VERIFIED | Public/private channels with shared weighted-blend math in `socialcore.js` `ReputationBook`; canonical `REPUTATION_UPDATE` events and `evaluationContext` decision consumption in `societycore.js`; pinned by `tests/reputation-public-private.test.js` (6/6, four negative controls) plus `tests/observation-rumor-latency-decay.test.js` for recipient-local trust resolution | Ongoing coverage |
| Routing/trade/economy | IMPLEMENTED_AND_VERIFIED | Merchant economy loop verified in `tests/routing-trade-economy.test.js`: MERCHANT_ECONOMY_CYCLE runs route+profitability plan → trip create → settle → two-sided price response as one parent-chained lineage; exact two-market stock conservation; WAIT and insufficient-stock rejections guarded; save/load seeded-identical continuation; production in `societycore.js` | Ongoing coverage |
| Convoys/escorts/bandits | IMPLEMENTED_AND_VERIFIED | Full loop verified in `tests/convoy-escort-bandit.test.js`: canonical parented events CONVOY_DISPATCH → CONVOY_BANDIT_THREAT → CONVOY_ESCORT_RESOLUTION → MARKET_TRIP_SETTLE; RNG-only rolls; save/load bit-for-bit continuation; market balance conserved; mid-loop deferral in `societycore.js` (worldStep skips convoy-owned trips) | Ongoing coverage |
| Factions/raids/escalation | IMPLEMENTED_AND_VERIFIED | raid loop production-wired (`tests/faction-raid-loop.test.js` 7/7: evaluation → dispatch → one-draw resolution, `raidUtility` scoring, `escalationLevel` surfaced, exact conservation) and chained from production decisions (`tests/faction-evaluation-raid-chain.test.js` 8/8: a DecisionCore RAID choice drives the loop as one TURN-rooted lineage) — the earlier "no proven faction loop" claim is superseded by this evidence; autonomous faction macro tick production-wired (`tests/faction-macro-tick.test.js` 7/7: `FACTION_MACRO_TICK` turns chained TURN-rooted, richest-other targets resolved at turn time, `worldStep({ factionTurns })` entry, save/load + determinism); retaliation loop production-wired (`tests/faction-retaliation-loop.test.js` 8/8: a raid RESOLUTION's outcome arms the victim, whose own production DecisionCore evaluation decides whether it strikes back, the counter-raid dispatching off the resolution that struck it at one spent depth level — grievance before/after, threshold, depth and eligibility recorded on every resolution); the grievance economy is production-wired (`RESP-FACTION-GRIEVANCE-ECONOMY-001`: fear feeds grievance at .5 per applied fear point, resentments renew on every gain — fear channel, raid resolutions, roaming shortages, injustices — and cool only while nothing renews them, 5 idle world ticks then .01/tick floored at zero, with the cooled count recorded on the TURN, so ordinary worlds reach the retaliation gate on their own and cool back below it; `tests/grievance-economy.test.js` 9/9 with 12/12 negative controls) | None identified for current scope |
| Crime/justice/legitimacy/migration | IMPLEMENTED_AND_VERIFIED | full crime→report→justice→legitimacy→migration loop through canonical events (`societycore.js` action kinds `CRIME_COMMIT`/`CRIME_REPORT`/`CRIME_JUSTICE`/`CRIME_MIGRATION`, settlement authority, parent-chained stages); `resolveCrime`/`reportCrime`/`accessToJustice`/`updateLegitimacy`/`shouldMigrate` now run in production; `tests/crime-justice-legitimacy-migration.test.js` | None identified for current scope |
| World tick / deterministic turns | IMPLEMENTED_AND_VERIFIED | `societycore.js` tick()/step(), monotonic event allocator with parentage, exact market flows, injectable serializable RNG; `tests/world-tick.test.js`; decay recorded on TURN, rumor queue/delivery as parented turn actions (`tests/event-causality-belief-path.test.js`); whole-history integrity audit `auditEventGraph()` — unique ids, seq continuity/mirror, parent seq+tick ordering (`tests/event-graph-audit.test.js`) | None identified for current scope |
| Market stock-and-flow conservation | IMPLEMENTED_AND_VERIFIED | `Market` ledger records production/destruction/filled/unmet/discard; `balanceSheet()` reconciles exactly; no creation from nowhere; `createTrip` duplicate-id guard fires before the stock check (invariant violation never masked as a business `null`); `tests/market-conservation.test.js` | None identified for current scope — pending-trip automation, cargo identity (owner/cargoKind preserved through delivery), and material accounting are covered by `tests/market-pending-trip-automation.test.js`, `tests/market-pending-trip-cargo-identity.test.js`, `tests/market-pending-trip-material-accounting-dynamic.test.js` |
| Determinism / RNG ownership | IMPLEMENTED_AND_VERIFIED | `randomcore.js` (mulberry32 + randomSource); Personality/DecisionCore draw from injectable serializable RNG defaults; belief/evidence clocks injectable with world-epoch fallback (no `Date.now` anywhere in production — source-scanned); dead `AgentBelief.decay()` (wall-clock) removed; `tests/determinism.test.js`, `tests/time-ownership.test.js` (3/3 incl. module source scan) | None identified for current scope |
| Event / rumor ID authority | IMPLEMENTED_AND_VERIFIED | RumorNetwork monotonic seq ids, duplicate rejection, `rumorSeq` save/load; `tests/rumor-identity.test.js`; RUMOR_QUEUE/RUMOR_DELIVER/RUMOR_RELAY ids monotonic and parented to the TURN (`tests/event-causality-belief-path.test.js`) | None identified for current scope |
| Campaign manager state | ACTIVE | `docs/CAMPAIGN_STATE.md` carries repo identity, RESP registry, open findings, next contract | Keep synchronized each wave |
| Character interactions | IMPLEMENTED_AND_VERIFIED | catalog executes end-to-end: `INTERACTION_EXECUTION` runs registered affordances through `AdvisoryGate` (only approval path), applies the declared effect table (transform AND runtime-registered recruit both verified to convert target type), records rejections without mutation, actor types persist through save/load; `tests/character-interaction-execution.test.js`; closes IMPLEMENTATION_STATUS item 6 | None identified for current scope |
| Advisory gate | IMPLEMENTED_AND_VERIFIED | `advisorygate.js` `AdvisoryGate.validate` is pure (no state mutation, `source: 'ADVISORY_VALIDATOR'`), production-wired as `SocietyCore.advisory`, emits canonical `ADVISORY_VALIDATION` events through `tick()`; pinned by `tests/interaction-causality.test.js`, `tests/interactioncore.test.js`, `tests/utility-affordance-runtime.test.js` | None identified for current scope |
| Player-to-invasion chain | IMPLEMENTED_AND_VERIFIED | Damage→fear→war→invasion chain verified in `tests/player-invasion-chain.test.js`: PLAYER_DAMAGE → FEAR_EVENT_RAISED → WAR_STATUS_UPDATE → INVASION_RESOLVED as one TURN-rooted lineage; pressure thresholds gate war status; invasion gated behind WAR with one world-RNG draw; loot conservation exact; save/load seeded-identical continuation; production in `societycore.js` | Ongoing coverage |
| Orphan modules | IMPLEMENTED_AND_VERIFIED | Import scan of all 8 modules: 0 orphans — `randomcore`/`decisioncore`/`interactioncore`/`advisorygate`/`macrocore`/`socialcore` imported by production, `utilitycore` imported by `decisioncore`/`interactioncore`, `societycore` is the root imported by 130+ tests; `.agents/fear-ai-autopilot.mjs` keep (campaign controller tooling), `persistence-probe.txt` archive (17-byte unreferenced probe, left in place); historical brain/fearcore/etc. modules absent from this checkout | None identified for current scope |
| VR/biofeedback | SOURCE_ABSENT | `vrsystem.js`, `biofeedback.js` and their tests absent (repo-wide glob: 0 matches); claim retracted under the evidence rule; both located upstream at fear-ai-sim/vrsystem.js (blob ac84c092aa94b5a32ed1bd2511974c7856e5f375) and fear-ai-sim/biofeedback.js (blob 050b8c4c8f72fe0c2d21919f73ab92e45c3fc4f4) — DEFERRED_PRODUCT_SCOPE, blob-pinned | Re-open only when the cited sources exist; product scope needs an explicit decision at that point; manifest + dispositions: docs/SOURCE_ABSENT_RECONCILIATION.md; guard: tests/source-absent-reconciliation.test.js |
| Neural fear | IMPLEMENTED_AND_VERIFIED | Re-opened 2026-09-23 (third re-open of the day) via the procedure in docs/SOURCE_ABSENT_RECONCILIATION.md: legacy sources extracted byte-exact to legacy/neuralfear.js (upstream blob b521978dcb05a29569bd51a905143f2e61c7e0be) and legacy/neuralnet.js (blob 3d320bb2f8fd6d23dd16c78bf785804cd418dc41), sha256s recorded in legacy/PROVENANCE.md and verified every gate; V8 integration NeuralFearModel in socialcore.js (ReLU hidden ladder + sigmoid output in [0,1], Xavier init, online gradient descent against the faction's ACTUAL fear, dropout, patience early stopping, running Welford normalization — draws from the injectable world RNG, LAZY initialization so seeded streams are untouched, world-time history) consumed in production by NEURAL_FEAR_PREDICT (read-only inference recording its error) and NEURAL_FEAR_LEARN (one online step), both canonical parent-chained events; model weights round-trip save/load. The loop is now CLOSED (`RESP-NEURAL-FEAR-LOOP-001`): `NEURAL_FEAR_FORECAST` lets a faction act on its own calibrated anticipation — only a model with enough observed samples AND an error inside tolerance may act, the signed `dread` between prediction and felt fear moves fear and the `opportunity` the production RAID candidate scores on (bounded by blend × dread, a maxStep cap and a deadband), an uncalibrated or deadband-sized discrepancy changes nothing, and the calibration bookkeeping (post-update no-dropout error EWMA) round-trips save/load — so a forecast demonstrably changes a real decision's input (RAID candidate .35 → .30) | tests/neural-fear-reopen.test.js (8/8) + tests/neural-fear-loop.test.js (10/10 with 8/8 negative controls): byte-exact extraction + provenance, lazy/draw-free initialization, architecture + saturating output, online learning/normalization/early stopping, save/load incl. the uninitialized case, production prediction + learning against world truth, guard ordering, whole-world determinism |
| CI | IMPLEMENTED_AND_VERIFIED | `.github/workflows/ci.yml` (Node 20, `npm ci` + `npm test` on push/PR) pinned by `tests/ci-workflow.test.js`; first remote run green — drizjet/fear-ai-sim Actions run 35874405000 on branch `main` (success, 2026-09-23); the earlier "root workflow added" retraction stands for the workspace-root claim | Observe subsequent runs |
| Documentation | PARTIALLY_IMPLEMENTED | multiple docs and this ledger; gate counts machine-agreed across all four orientation docs incl. the IMPLEMENTATION_STATUS `Current gate` pointer (drift to 142/405 caught and guarded 2026-09-22); corpus assertion quality is itself guarded (`RESP-ASSERTION-QUALITY-GUARD-001` — `tests/assertion-quality.test.js` fails any suite containing an unprovable assertion signature: empty `toMatchObject`, a literal `expect(true)`, a `?.` actual under a matcher `undefined` satisfies, an assertion inside `catch`, or an `it` with no assertion) | Must stay synchronized after implementation |
| Knowledge DB writeback | BLOCKED_EXTERNAL | no database artifact in this checkout (scan: no db/sqlite files); write path is external by definition and not verifiable from this repository | Requires database access outside this checkout; recorded as an external dependency |

## Completion policy

This ledger intentionally does not mark the project complete. Standalone classes, unit tests, and successful builds do not satisfy runtime integration requirements. Each item must progress through implementation, wiring, cross-system tests, runtime verification, documentation, and provenance recording.
