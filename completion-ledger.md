# Fear AI Completion Ledger

Updated 2026-09-22 from the current checkout. Status is based on source/runtime/test evidence, not historical claims.

| Area | Status | Evidence | Remaining |
|---|---|---|---|
| Simulation/agents/combat | SOURCE_ABSENT | `simulation.js`, `agent.js`, `learningagent.js` and their integration tests absent from this checkout (repo-wide glob: 0 matches); former IMPLEMENTED_AND_VERIFIED claim retracted under the evidence rule; the society world's actors are the only agent runtime present (no combat subsystem; societycore owns them) | Re-open only when the cited sources exist; combat needs an implement-and-test pass of its own |
| FearCore live transitions | SOURCE_ABSENT | `fearcore.js`, `brain.js`, `tests/fearcore.test.js` absent (repo-wide glob: 0 matches); no live transition adapter or threshold tests here; `Personality.fearSensitivity()` (socialcore) is the only live fear primitive; no authoritative FearBand source found anywhere in the workspace | Re-open only when the cited sources exist; Rust parity requires a real FearBand source |
| Brain scale cleanup | SOURCE_ABSENT | claim rested on the historical brain module and legacy threshold tests, all absent (0 matches); no brain module exists in this checkout to clean; retracted under the evidence rule | Re-open only when the cited sources exist |
| Habituation | SOURCE_ABSENT | `brain.js` and `habituation.js` absent (repo-wide glob: 0 matches); source scan finds habituation only in this ledger and dated docs — no implementation in any checkout module | Re-open only when the cited sources exist; canonical-owner question is moot while no implementation exists here |
| Hysteresis | SOURCE_ABSENT | `fearcore.js` and `hysteresis.js` absent (repo-wide glob: 0 matches); source scan finds hysteresis only in this ledger and dated docs | Re-open only when the cited sources exist |
| Utility/affordances | IMPLEMENTED_AND_VERIFIED | `utilitycore.js` (`AffordanceRegistry` + `UtilityRuntime`) owns registration, type gating, curve/weight scoring, and band selection; `decisioncore`/`interactioncore`/`advisorygate` delegate with unchanged API and RNG order; production paths (society DECISION/FACTION_EVALUATION/INTERACTION_EVALUATION, advisory gate) run through it; `tests/utility-affordance-runtime.test.js` | None identified for current scope |
| Personality | IMPLEMENTED_AND_VERIFIED | `socialcore.js` `Personality` is the sole owner (the historical brain module is absent from this checkout): OCEAN + derived traits, world-RNG draws via world registry `setPersonality`; flows into DECISION where the class owns `decisionBandWidth` (plain-object fallback preserved); revives as a class instance across save/load; `tests/personality-morale-ownership.test.js` | None identified for current scope |
| Morale | IMPLEMENTED_AND_VERIFIED | `socialcore.js` `Morale` is the sole owner (the historical brain module is absent from this checkout): world registry `setMorale` + `MORALE_UPDATE` → canonical `MORALE_SHIFT` TURN children (before/after/delta/freezeRisk); morale flows into DECISION considerations via `evaluationContext`; revives as a class instance across save/load; `tests/personality-morale-ownership.test.js` | None identified for current scope |
| Beliefs/evidence | IMPLEMENTED_AND_VERIFIED | observation→belief (`recordRouteObservation`) and rumor→belief (`spread`/`deliverDue`/`relayRumor`) production paths pinned; recipient-local trust (`trustFor`/reputation/skepticism); latency/decay/distortion suite `tests/observation-rumor-latency-decay.test.js`; belief paths through canonical event emission with decision provenance (`tests/event-causality-belief-path.test.js`) | None identified for current scope |
| Rumors | IMPLEMENTED_AND_VERIFIED | monotonic ids (auto ids skip explicitly reserved ones — an explicit `rumor-1` can no longer poison the counter), delayed delivery with latency decay, TTL expiry, distortion, relay-through-belief, save/load incl. `confidenceHalfLife`; `tests/rumor-identity.test.js`, `tests/rumor-transport.test.js`, `tests/observation-rumor-latency-decay.test.js` | Public/private reputation flows over delivered rumor evidence |
| Reputation/trust | PARTIALLY_IMPLEMENTED | `socialcore.js` `ReputationBook` gates rumor evidence weight via recipient-local trust resolution (the former socialdynamics citation was stale — file absent from checkout); pinned by `tests/observation-rumor-latency-decay.test.js` | Public/private reputation flows and decision consumption |
| Routing/trade/economy | IMPLEMENTED_AND_VERIFIED | Merchant economy loop verified in `tests/routing-trade-economy.test.js`: MERCHANT_ECONOMY_CYCLE runs route+profitability plan → trip create → settle → two-sided price response as one parent-chained lineage; exact two-market stock conservation; WAIT and insufficient-stock rejections guarded; save/load seeded-identical continuation; production in `societycore.js` | Ongoing coverage |
| Convoys/escorts/bandits | IMPLEMENTED_AND_VERIFIED | Full loop verified in `tests/convoy-escort-bandit.test.js`: canonical parented events CONVOY_DISPATCH → CONVOY_BANDIT_THREAT → CONVOY_ESCORT_RESOLUTION → MARKET_TRIP_SETTLE; RNG-only rolls; save/load bit-for-bit continuation; market balance conserved; mid-loop deferral in `societycore.js` (worldStep skips convoy-owned trips) | Ongoing coverage |
| Factions/raids/escalation | PARTIALLY_IMPLEMENTED | tribal systems exist; no full specified faction loop proven | Implement estimates, escalation, raids, retaliation |
| Crime/justice/legitimacy/migration | IMPLEMENTED_AND_VERIFIED | full crime→report→justice→legitimacy→migration loop through canonical events (`societycore.js` action kinds `CRIME_COMMIT`/`CRIME_REPORT`/`CRIME_JUSTICE`/`CRIME_MIGRATION`, settlement authority, parent-chained stages); `resolveCrime`/`reportCrime`/`accessToJustice`/`updateLegitimacy`/`shouldMigrate` now run in production; `tests/crime-justice-legitimacy-migration.test.js` | None identified for current scope |
| World tick / deterministic turns | IMPLEMENTED_AND_VERIFIED | `societycore.js` tick()/step(), monotonic event allocator with parentage, exact market flows, injectable serializable RNG; `tests/world-tick.test.js`; decay recorded on TURN, rumor queue/delivery as parented turn actions (`tests/event-causality-belief-path.test.js`); whole-history integrity audit `auditEventGraph()` — unique ids, seq continuity/mirror, parent seq+tick ordering (`tests/event-graph-audit.test.js`) | None identified for current scope |
| Market stock-and-flow conservation | IMPLEMENTED_AND_VERIFIED | `Market` ledger records production/destruction/filled/unmet/discard; `balanceSheet()` reconciles exactly; no creation from nowhere; `createTrip` duplicate-id guard fires before the stock check (invariant violation never masked as a business `null`); `tests/market-conservation.test.js` | Build pending-trip cargo (commitment → exposure → arrival) on top |
| Determinism / RNG ownership | IMPLEMENTED_AND_VERIFIED | `randomcore.js` (mulberry32 + randomSource); Personality/DecisionCore draw from injectable serializable RNG defaults; belief/evidence clocks injectable with world-epoch fallback (no `Date.now` anywhere in production — source-scanned); dead `AgentBelief.decay()` (wall-clock) removed; `tests/determinism.test.js`, `tests/time-ownership.test.js` (3/3 incl. module source scan) | None identified for current scope |
| Event / rumor ID authority | IMPLEMENTED_AND_VERIFIED | RumorNetwork monotonic seq ids, duplicate rejection, `rumorSeq` save/load; `tests/rumor-identity.test.js`; RUMOR_QUEUE/RUMOR_DELIVER/RUMOR_RELAY ids monotonic and parented to the TURN (`tests/event-causality-belief-path.test.js`) | None identified for current scope |
| Campaign manager state | ACTIVE | `docs/CAMPAIGN_STATE.md` carries repo identity, RESP registry, open findings, next contract | Keep synchronized each wave |
| Character interactions | IMPLEMENTED_AND_VERIFIED | catalog executes end-to-end: `INTERACTION_EXECUTION` runs registered affordances through `AdvisoryGate` (only approval path), applies the declared effect table (transform AND runtime-registered recruit both verified to convert target type), records rejections without mutation, actor types persist through save/load; `tests/character-interaction-execution.test.js`; closes IMPLEMENTATION_STATUS item 6 | None identified for current scope |
| Advisory gate | IMPLEMENTED_AND_VERIFIED | `advisorygate.js` `AdvisoryGate.validate` is pure (no state mutation, `source: 'ADVISORY_VALIDATOR'`), production-wired as `SocietyCore.advisory`, emits canonical `ADVISORY_VALIDATION` events through `tick()`; pinned by `tests/interaction-causality.test.js`, `tests/interactioncore.test.js`, `tests/utility-affordance-runtime.test.js` | None identified for current scope |
| Player-to-invasion chain | IMPLEMENTED_AND_VERIFIED | Damage→fear→war→invasion chain verified in `tests/player-invasion-chain.test.js`: PLAYER_DAMAGE → FEAR_EVENT_RAISED → WAR_STATUS_UPDATE → INVASION_RESOLVED as one TURN-rooted lineage; pressure thresholds gate war status; invasion gated behind WAR with one world-RNG draw; loot conservation exact; save/load seeded-identical continuation; production in `societycore.js` | Ongoing coverage |
| Orphan modules | IMPLEMENTED_AND_VERIFIED | Import scan of all 8 modules: 0 orphans — `randomcore`/`decisioncore`/`interactioncore`/`advisorygate`/`macrocore`/`socialcore` imported by production, `utilitycore` imported by `decisioncore`/`interactioncore`, `societycore` is the root imported by 130+ tests; `.agents/fear-ai-autopilot.mjs` keep (campaign controller tooling), `persistence-probe.txt` archive (17-byte unreferenced probe, left in place); historical brain/fearcore/etc. modules absent from this checkout | None identified for current scope |
| VR/biofeedback | SOURCE_ABSENT | `vrsystem.js`, `biofeedback.js` and their tests absent (repo-wide glob: 0 matches); claim retracted under the evidence rule | Re-open only when the cited sources exist; product scope needs an explicit decision at that point |
| Neural fear | SOURCE_ABSENT | "module and tests exist" not supported: no neural-fear module or test present (glob 0 matches, no `neural` references in any checkout module); claim retracted under the evidence rule | Re-open only when the cited sources exist; wire/defer decision deferred with the sources |
| CI | PARTIALLY_IMPLEMENTED | `.github/workflows/ci.yml` now exists (Node 20, `npm ci` + `npm test` on push/PR) and is pinned by `tests/ci-workflow.test.js`; the earlier "root workflow added" retraction stands for the workspace-root claim; remote execution unverified until a remote accepts the push (external) | Observe the first remote run once pushed (external) |
| Documentation | PARTIALLY_IMPLEMENTED | multiple docs and this ledger; gate counts machine-agreed across all four orientation docs incl. the IMPLEMENTATION_STATUS `Current gate` pointer (drift to 142/405 caught and guarded 2026-09-22) | Must stay synchronized after implementation |
| Knowledge DB writeback | BLOCKED_EXTERNAL | no database artifact in this checkout (scan: no db/sqlite files); write path is external by definition and not verifiable from this repository | Requires database access outside this checkout; recorded as an external dependency |

## Completion policy

This ledger intentionally does not mark the project complete. Standalone classes, unit tests, and successful builds do not satisfy runtime integration requirements. Each item must progress through implementation, wiring, cross-system tests, runtime verification, documentation, and provenance recording.
