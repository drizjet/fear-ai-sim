{
  "name": "fear-ai-sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic fear-AI simulation runtime (SocietyCore world/events, UtilityCore shared scoring, DecisionCore, InteractionCore, AdvisoryGate, SocialCore, MacroCore, RandomCore).",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "test:watch": "node --experimental-vm-modules node_modules/jest/bin/jest.js --watch",
    "gate:sync": "node tools/gate-counts.mjs",
    "gate:check": "node tools/gate-counts.mjs --check"
  },
  "devDependencies": {
    "@codebuff/sdk": "^0.10.7",
    "jest": "^29.7.0"
  }
}
